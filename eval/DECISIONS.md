# Décisions tranchées

> **Ce qui a été tranché sur la PHOTOTHÈQUE** — reconnaissance, visibilité,
> triage, stockage, renommage, lieux, recherche, dates. Chaque piste, son
> verdict, pour ne rien re-proposer.
>
> **TROIS carnets frères, découpés par DOMAINE** — le réflexe « ne rien
> re-proposer » est lui-même par domaine, et qui travaille la recherche n'a
> jamais besoin de savoir pourquoi `taskkill` a échoué :
> · `docs/DECISIONS_OUTILLAGE.md` (20/08) — les trois canaux, le pilotage,
>   la livraison git, le pont vers le poste de Mike ;
> · `eval/DECISIONS_UI.md` (07/09) — l'écran : composants, cibles, contraste ;
> · `eval/DECISIONS_TAGGING.md` (08/09) — ce que le MODÈLE écrit : prompts,
>   vocabulaire, campagnes, dictionnaire FR→EN.
> Ce que la photothèque FAIT des tags — recherche, filtres, axes — est ici.
>
> Détail des travaux : git. La méthode : `eval/METHODE.md`. Les adoptés
> stabilisés : Acquis du `ROADMAP.md`.

## Reconnaissance, triage, stockage

| Idée / piste | Verdict | Raison |
|---|---|---|
| Garde humain/animal auto (SigLIP 0-shot) | **REJETÉ** (08/08) | 18 % faux rejets. |
| **La réparation des XMP ATTEND la file du serveur au lieu d'abandonner** | **ADOPTÉ, OBSERVÉ** (23/08, `appliquer_xmp_personnes --tous`) | La passe s'arrêtait au premier signe de `PERSON_QUEUE` — invariant « jamais deux écrivains ». Or **le curateur rattache des visages TOUT SEUL toutes les 4 à 5 minutes** (`AUTO_ADD_ENABLE`), et chaque auto-ajout remplit la file quelques secondes. Lancée à 21:38, la passe est morte à **22:09:40, onze secondes après « Auto-ajout : 14 visage(s) » de 22:09:29** — **4 800 photos sur 18 900**, proprement, sans personne pour la relancer. Une passe de cinq heures qui renonce sur un événement qui revient toutes les quatre minutes ne finira jamais ; invisible jusque-là parce qu'aucune passe n'avait tourné assez longtemps. Elle ATTEND désormais que la file retombe à zéro, **patience bornée à 30 min** (un script qui se fige est pire qu'un script qui échoue — leçon du `{ready}` avalé par `-q`), et le temps attendu est COMPTÉ dans le rapport : une passe deux fois plus lente sans qu'on sache pourquoi est une mesure fausse. **L'invariant n'a pas bougé d'un pouce** — un test vérifie qu'AUCUNE invocation d'ExifTool n'a lieu pendant que la file travaille : attendre n'est pas écrire. `--patience 0` rend l'ancien refus immédiat. **7 fonctions de test neuves, 7 rouges sur l'ancien code, 56/56 vertes sous Windows.** |
| **Couper l'auto-ajout du curateur pendant la réparation** | **REJETÉ** (23/08) | `seuils.txt` peut porter `AUTO_ADD_SIM = 1.01`, ce qui rendrait tout auto-ajout impossible : un fichier, réversible, sans code. Mais c'est un seuil MESURÉ (30/07 : 99 % des candidats passent en automatique) qu'on tordrait pour contourner un défaut d'outil, et il faudrait un redémarrage à l'aller comme au retour — deux occasions d'oublier de le remettre. Faire attendre l'outil coûte moins cher et ne touche à aucun réglage du produit. |
| **« Vérité terrain 0,8 % » mélangeait deux mesures** | **CORRIGÉ, PARQUÉ** (21/08, choix de Mike) | Ce dont le PRODUIT a besoin : **18 863** photos portent un `personne:` (44,8 % du fonds vivant, 352 noms) — les gens qu'on connaît sont couverts. Ce dont un ALGORITHME a besoin : **1 196** visages rattachés sur **71 868** (1,66 %). Seul le chantier 9 dépend du second. Et le compte était incomplet : les **1 496 exclusions** sont des étiquettes humaines — « ce visage n'est PAS Flo » évalue un clustering aussi bien qu'un rattachement. Vérité terrain réelle : **2 984 décisions**, pas 1 196 — 1 496 exclusions et **292 confirmations** s'ajoutent aux 1 196 rattachements. Sous-comptée deux fois le même jour : d'abord en oubliant les négatifs, puis en oubliant les confirmations. |
| **La propagation des noms a CONVERGÉ** | **MESURÉ** (21/08, `mesure_propagation_noms.py`) | Le chantier 16(a) supposait un mécanisme à écrire ; il tourne déjà (`build_suggestions`, passe intégrale toutes les 240 s, `AUTO_ADD` à 0,40 avec marge 0,10). Ce qu'il rattacherait MAINTENANT sur 71 461 visages : **14 en automatique, 24 en file — 33 photos, 38 noms**. Rien n'est caché par le plafond (38 cartes sur 400). Le banc et le serveur VIVANT donnent les trois mêmes nombres (`remove` 13, `merge` 1, `add` 24) : c'est ce qui autorise à conclure. |
| **Le cas exact du chantier 16 pèse 17 photos** | **MESURÉ → 16(a) CLOS** (21/08) | « Une photo porte Florine et Caline, Flora s'ajoute » : **18 745** photos ont un nom posé ET au moins un visage non couvert (le 6 287 du 20/08 était une borne basse), mais seulement **17** gagneraient un nom neuf. Il n'y a pas de matière — ni dans le modèle, ni dans l'UI. 16(b) était déjà acquis ; reste 16(c), le seul étage LLM. |
| **Le brut annonçait 3 698 rattachements en attente** | **FAUX à 99,6 %** (21/08) | Sans le garde-fou des clés fantômes, le banc comptait 3 698 candidats « auto ». Testées une à une contre le NAS : **3 684 pointent vers un fichier qui n'existe plus**. Leçon de forme, pas de fond : un banc qui ne réplique pas les garde-fous du producteur mesure un cousin de la prod. C'est le `--fichiers` qui a fait tomber 3 698 à 14. |
| **La purge du 17/08 n'a traité qu'un magasin sur deux** | **MESURÉ** (21/08) | Le magasin de visages garde **2 374** fiches dont la clé n'est plus dans l'index — **exactement** les 2 374 clés purgées le 17/08 (intersection 2 374 / 2 374 contre `_corbeille_vecteurs/vecteurs_orphelins_20260817_073335.jsonl`). Leurs vecteurs SigLIP sont partis, leurs VISAGES sont restés. Depuis, le curateur les re-score toutes les 240 s et le garde-fou les rejette en silence. Un incident clos par son symptôme. |
| **125 décisions humaines vivent sur des clés oubliées** | **MESURÉ — à sauver AVANT toute purge** (21/08) | Sur ces 2 374 clés : **104 rattachements**, **11 exclusions**, **10 confirmations** (Alix Baudère, Luna…). Elles comptent dans la vérité terrain de 2 984 recensée la veille. La règle 2 du projet impose l'ordre : sauver, puis purger — jamais l'inverse. |
| **Le réservoir sous le seuil n'est pas un gisement de noms** | **MESURÉ** (21/08) | 28 684 visages sous 0,40 ; meilleur voisin **médian 0,21**, p90 0,311. Ce n'est pas le seuil qui les retient : ce sont des gens sans fiche. La tranche 0,35–0,40 pèse 1 328 visages (1 106 photos vivantes) — abaisser le seuil ne se décidera que sur un jugement humain de 30 propositions, jamais sur le volume. |
| **La CAUSE : la cascade est pilotée par l'INDEX, et l'index a déjà oublié** | **TROUVÉE, MESURÉE** (21/08) | `_sync_dir` calcule ses orphelins À PARTIR de `STORE` : une clé déjà absente de l'index lui est **invisible**, et `forget_everywhere` ne sera jamais appelé pour elle. `purge_cles_fantomes`, l'autre filet, exige un **jumeau vivant** de même nom de fichier — quand les deux jumeaux sont morts (`ARZOPA/x` et `…\_Uploads\ARZOPA\x`), elle ne se déclenche jamais. Classement des 2 374 : **2 283 « personne ne les voit »**, **91 sous un chemin caché** (`.corbeille-rangement`), que la purge de démarrage (`demarrage:dossiers_caches`) retire de l'index par `STORE.remove_many` **sans cascade**. Deux fuites, un même effet. |
| **Il y avait un TROISIÈME orphelin, et aucun instrument ne le regardait** | **CORRIGÉ** (21/08, `verifier_orphelins.analyser_hors_index`) | Le diagnostic comparait les détections au **DISQUE** et les vecteurs à l'**INDEX** — jamais les détections à l'index. C'est exactement l'angle où les 2 374 se sont logées, et pourquoi la purge du 17/08 a pu « observer 0 muet » en laissant un magasin sur deux. Observé sur la base vivante : `faces` **2 374** hors index sur 44 450, `animals` **2 377** sur 44 453, **120 clés** portant une décision humaine, et **0** vecteur orphelin — le 17/08 avait bien fait son travail, sur son seul magasin. `--sans-disque` rend le contrôle lançable par un banc en **0,7 s**. |
| **Le correctif : la cascade rattrape ce que l'index a oublié** | **ADOPTÉ, OBSERVÉ** (21/08) | Deux gestes. (1) La purge de démarrage des dossiers cachés appelle `forget_everywhere` au lieu de `STORE.remove_many` — préventif, c'est la fuite qui créait les 91. (2) `purge_detections_hors_index()` balaie au démarrage ce que `_sync_dir` ne peut plus voir, avec **deux garde-fous** : jamais une clé portant une décision humaine, et **seulement** ce que l'index ne reprendra jamais (fichier absent ou chemin caché) — une clé dont le fichier existe encore est en attente de re-tagging et se compte au lieu de se purger. Quarantaine JSONL avant tout retrait. **Le dry-run a prédit 2 254 + 2 257, la quarantaine en contient exactement 2 254 + 2 257** ; observé après redémarrage : `visages` **44 450 → 42 196**, hors index **2 374 → 120** (les protégées), file du curateur **inchangée** (13/1/24), index intact à 43 065. |
| **« 787 décisions déjà perdues » — RÉFUTÉ** | **CHIFFRE NEUF** (22/08, `mesure_report_orphelines.py`) | Personne ne leur avait cherché de jumeau : le banc du 21/08 ne fouillait que les 141 clés encore présentes dans le magasin de visages, et déclarait les 787 autres perdues **par défaut de recherche**, pas par mesure. En suivant les journaux d'annulation de `docs/`, **698** des **804** clés mortes retrouvent leur photo. Sur **928** décisions orphelines (596 rattachements, 249 exclusions, 83 confirmations), **748 se re-clent**, 56 y sont déjà, **124** sont vraiment perdues. Le « 1 seule décision à reporter (Luna) » du 21/08 tombe avec. |
| **LA CAUSE des 928 : `rekey_everywhere` ne re-clé pas les DÉCISIONS** | **TROUVÉE, CORRIGÉE** (22/08) | Sept magasins transportés, quatre « stores de sujets » dans la même boucle — mais `PEOPLE` et `PETS` sont les seuls keyés par **NOM** : leurs chemins vivent DANS la fiche (`faces` = [[chemin, index]], `exclude`, `confirmed`, `avatar`). `store.rekey(ancien_chemin, nouveau_chemin)` y cherche une entrée dont la CLÉ serait un chemin, n'en trouve jamais, renvoie faux **et ne dit rien**. La boucle avait l'air de couvrir quatre magasins ; elle en couvrait deux. Chaque rangement par année et chacun des 7 058 renommages a donc décroché des décisions. Le TAG survivait (index + XMP) — la règle 2 tenait, la photo gardait son nom ; c'est la VÉRITÉ TERRAIN qui partait, et une exclusion perdue est un faux positif qui revient. Correctif : `recle_decisions.py`, règle pure, 15 tests, branchée dans `rekey_everywhere`. |
| **Le re-clé rétroactif : APPLIQUÉ, et aucune décision perdue** | **OBSERVÉ** (22/08, geste de Mike) | **787 décisions sur 685 clés, 97 fiches**. Décisions posées sur une clé hors index : **928 → 140**. La vérité terrain passe de 3 364 à **3 310**, et les 54 manquantes sont des DOUBLONS fusionnés, pas des pertes : l'audit de la quarantaine apparie chaque sortie à une arrivée de même type et de même index — 788 sorties, **734 appariées**, 54 fusions, **0 sans contrepartie**. Règle 2 tenue. |
| **Le résidu : GARDÉ** | **CHOIX DE MIKE** (22/08) | 140 décisions sur des clés dont aucun journal ne connaît la destination (117 rattachements, 13 exclusions, 10 confirmations) + les 120 clés protégées de la purge du 21/08. Les purger est irréversible, les garder ne l'est pas — et le jour même, 787 décisions déclarées « déjà perdues » se sont révélées récupérables dès qu'une preuve neuve est apparue. Ne pas rouvrir sans chiffre neuf. |
| **Les journaux d'annulation sont la meilleure preuve d'identité du projet** | **ADOPTÉ** (22/08, `journaux_deplacements.py`) | Trois chemins ont été comparés pour dire « cette photo est devenue celle-là » : le NOM de fichier en retrouve **346** clés sur 804 (et refuse 36 homonymes), le VECTEUR **13** seulement — la purge du 21/08 ayant emporté les détections des clés mortes —, le JOURNAL **685**. Ce n'est pas une ressemblance : c'est le geste lui-même, écrit par le programme qui l'a fait (`old_key` → `new_key`, `canonique` pour un doublon absorbé), 19 331 déplacements connus. Module partagé par le serveur et le banc : une seule lecture des journaux dans le projet. |
| **L'index d'une vignette survit au re-clé, et ça se contrôle** | **MESURÉ** (22/08) | Re-clé `[ancien, 3]` en `[nouveau, 3]` n'est légitime que parce que `rekey_everywhere` déplace la liste des détections EN BLOC. Le contrôle : l'index doit tomber dans les détections de la cible. Sur 462 rattachements re-clables, **0 hors bornes** — corroboration indépendante des jumeaux, un jumeau faux ferait déborder les index. |
| **La vérité terrain est de 3 364 décisions, pas 2 984** | **CORRIGÉ, troisième fois** (21/08) | 1 576 rattachements (et non 1 196 : **1 196 comptait des CLÉS**, or un rattachement est `[clé, index]` — plusieurs par photo), 1 496 exclusions, 292 confirmations. La même confusion clé/visage que le 21/08 au matin, à un étage de plus. |
| **Le registre des oublis meurt à chaque redémarrage** | **CORRIGÉ, OBSERVÉ** (22/08) | `comptes_index` a été bâti le 17/08 pour rendre les −250 diagnosticables APRÈS coup. Il vit en MÉMOIRE : après le redémarrage de 19:31, `par_motif` est vide et aucun cycle ne bouge. La cause des 2 283 ne pourra donc jamais être établie rétrospectivement — seulement la cause STRUCTURELLE, lue dans le code. « Ce qui n'est pas compté n'est pas diagnosticable après » : il faut encore que le compte SURVIVE. **Correctif (22/08)** : `etat()` / `restaurer()` — le module reste PUR (aucune E/S), c'est `server.py` qui écrit `_comptes_index.json`, atomiquement. Écrit **dès le démarrage** et non seulement en fin de cycle : un premier scan dure des minutes, et deux redémarrages rapprochés n'en laissaient jamais finir un — la même leçon que `_backup_du()`, dont l'échéance se lit sur le FICHIER. Observé : deux redémarrages, `redemarrages` 0 → 1, `depuis` conservé. Au passage, `cycles_vus` cesse d'afficher « 10 » à vie (la liste est bornée à 10 ; le compteur, non). |
| **Ce que vaut la tranche 0,35–0,40** | **MESURÉ, TRANCHÉ** (22/08, `--bilan`) | 30 propositions tirées uniformément, jugées par Mike : **25 justes, 2 fausses, 3 indécidables** — **92,6 %** sur 27 tranchées, **Wilson 95 % : 76,6 % – 97,9 %**. Ce que l'intervalle autorise, et rien de plus : la tranche a sa place dans la file **« À vérifier »**, **jamais dans l'auto-ajout**. Abaisser `CUR_ADD_SIM` reste donc refusé ; 30 tirages ne sont pas un pourcentage, et une borne basse à 76,6 % interdit l'automatique. |
| **La planche de référence de `/tranche` était FIGÉE** | **DÉFAUT TROUVÉ, CORRIGÉ, OBSERVÉ** (22/08) | Mike : « je n'ai pas constaté d'amélioration, certaines photos proposées pour une personne contiennent une AUTRE personne. » Le tirage avait été écrit à **21:26**, le recalage appliqué à **22:19** : la page servait les `refs` du FICHIER, donc l'état d'avant la réparation. Croisé avec la quarantaine du recalage : **3 planches sur 30 périmées** — Didier, Mathieu, Markus Grossert, et les deux premières sont exactement celles qu'il avait signalées la veille. Sur celles-là, aucune amélioration n'était possible : c'était l'image d'avant. Correctif : `_tranche_refs_vivantes` relit la fiche à chaque affichage, le banc ne tire plus de références du tout (elles appartiennent à la PAGE). Observé après redémarrage (`code_a_jour` vrai) : **4 références corrigées sur les 3 planches** — Didier `20230409_151644` #1→#0, Markus `…125257` #7→#5 et `…125303` #9→#10, Mathieu `DSC01607` #0→#1 — les mêmes 4 que la quarantaine, deux chemins. Les 30 verdicts ont survécu au redémarrage. Au passage la légende cesse de mentir : « visages déjà **rattachés à** X », `confirmed` étant un autre champ — une planche qui se dit *confirmée* et montre un intrus accuse le jugement humain, alors que c'est l'index qui a glissé. |
| **Le résidu du recalage est CONCENTRÉ, et le taux le cachait** | **MESURÉ** (22/08) | Après réparation, 0,8 % de couples décalés se lit comme un fonds sain. Par FICHE, c'est autre chose : les **9 décalés** et les **34 refus « ambigu »** tiennent sur **10 fiches**, et **Didier en porte 4 des 9**. Sa fiche cite deux visages de la MÊME photo (i=1 à 0,908 et i=8 à 0,745) : la règle refuse exprès de trancher, et ce n'est pas un recalage qu'il faut mais un **retrait** — donc un geste humain. Par POSTE dans la fiche, ce que la planche montre : poste 0 → 1 décalé + 2 faux, poste 1 → 1 décalé + 2 faux, sur 104 fiches. C'est la matière du point 1bis. |
| **Le résidu se juge à l'œil, et la page ne doit rien faire d'autre que collecter** | **LIVRÉ, OBSERVÉ** (22/08, `/residu` + `--residu` / `--bilan-residu`) | **15 cas sur 9 fiches, 34 couples cités** (Didier 4, Res Jordi 4, puis Céline Gauchat, Flo, Jenny, Maryline Baudère, Rosario, Sylvie Chatelain, Val). Trois choix de conception, chacun payé par une leçon du projet : (1) la planche de référence est **vivante** ET **exclut la photo en cause** — juger un visage contre lui-même ne mesure rien ; (2) un verdict ne peut désigner qu'un visage **montré** (400 sinon), sans quoi une décision humaine se poserait sur une vue qui n'a pas eu lieu ; (3) le candidat non cité n'apparaît que s'il **bat le plus faible des cités** — en dessous ce n'est pas un prétendant, c'est une vignette de plus, et sur une page de jugement l'attention est la ressource rare. La page **ne retire rien** : `--bilan-residu` sépare *à retirer* / *confirmé* / *à AJOUTER* (une attribution, autre geste), et le retrait revient dans `/reglages`. Observé en réel : 15 cas servis, planches vivantes, sélection sans écriture. |
| **Le résidu du recalage : JUGÉ, et il n'y avait rien à retirer** | **CLOS par le jugement humain** (22/08) | Mike a jugé les **15 cas** : **34 couples confirmés, 0 à retirer, 0 indécidable**. Un test géométrique du banc a d'abord déclaré ce résultat impossible (boîtes disjointes = deux personnes, 0,0 sur 15/15) — **le test avait tort** : la première photo ouverte est une **page d'album photographiée**, cinq tirages dans un fichier, et `Flyer_Jenny.jpg` porte quatre portraits de Jenny. Un fichier n'est pas une scène. Les refus « ambigu » de `recale_rattachements` étaient donc justes, et la règle qui « refuse plus qu'elle ne répare » est validée par l'œil humain sur toute la population qu'elle a refusée. |
| **Les 33 recalages appliqués : 29 réparations, 4 rebrassages** | **MESURÉ** (22/08, `--verifier-recalages`) | Le critère « décalé » est un ÉCART de score, pas une identité fausse — sur une page d'album il sépare deux apparitions de la même personne. Le juge : le score de l'ANCIEN index. **29 sur 33 (87,9 %) étaient sous `CUR_FP_SIM`**, jusqu'à **−0,13** — l'ancien visage ne ressemblait à personne, c'était bien une réparation. **4 rebrassages** (12,1 %), tous sur une rafale de Flo. Le « 42 → 9 » tient donc pour ce qui a été appliqué ; c'est le MOT qui était trop fort, pas le geste. |
| **Le vrai défaut restant : 13 couples sous le seuil de faux positif** | **NOMMÉ, à traiter** (22/08) | Il était compté dans le rapport depuis le premier jour, jamais listé. Score **0,06 à 0,295** : Flo à **0,06**, Linda posée sur un fichier `Sanchez Laura.jpg`, Markus sur `117-1798_HUM Mutz.JPG` (un nom d'ANIMAL — cross-pipeline). Et la plupart sont sur des photos à **un seul visage** : aucun recalage ne peut les réparer, il n'y a nulle part où aller. Ils demandent un **retrait**, donc un jugement humain puis un geste de Mike. C'est la population sur laquelle `/residu` doit maintenant pointer. |
| **La page de jugement montre la SCÈNE, pas seulement le crop** | **ADOPTÉ, OBSERVÉ** (22/08) | Trois corrections payées par la séance du jour. (1) **La photo entière avec les visages encadrés** : un crop de 200 px détaché ne dit pas si le fichier est une scène ou une page d'album — c'est ce qui a coûté une conclusion fausse. Les cadres sont posés en POURCENTAGE (`_boite_en_fractions`) et `_dimensions_photo` lit l'orientation EXIF, les `bbox` vivant dans l'espace redressé que `exif_transpose` produit chez les deux autres producteurs ; sans ça, tout portrait vertical recevrait un cadre pivoté. (2) **Des lettres A–H, jamais des chiffres** : `/tranche` utilise `1`/`2`/`3` pour Oui/Non/Je ne sais pas, et les mêmes touches à dix minutes d'intervalle avec un sens opposé sont un piège, pas une commodité. (3) **Le bouton dit la CONSÉQUENCE** — « Valider écrira : 0 gardé · 1 à RETIRER » — au lieu du nombre de cases cochées : c'est ce que la validation produit qui compte. Le garde-fou géométrique envisagé (« deux boîtes disjointes = deux personnes ») a été **abandonné** : il est faux sur les pages d'album, et une page qui second-devine l'humain sur une règle fausse est pire que pas de garde-fou. |
| **Les 13 « faux positifs » : 12 étaient JUSTES** | **MESURÉ, et le verdict porte sur l'INSTRUMENT** (22/08) | Mike a jugé les 13 couples dont le visage désigné score **0,06–0,295** : il en a confirmé **12**. Flo à 0,06, FX à 0,113, Markus à 0,135 — ce sont ses photos, il les reconnaît. Donc la colonne « sous le seuil de faux positif » **ne nomme pas des rattachements faux** : elle nomme les visages sur lesquels l'EMPREINTE échoue — petits, flous, de profil, scannés. Ce n'était pas un défaut du fonds, c'était un aveu du modèle, et il faut le lire comme tel : ce chiffre mesure la cécité de la signature. Corollaire à garder : une empreinte de ce genre entrée dans `refs` blanchit ensuite son propre couple (la BORNE BASSE du banc). |
| **Le retrait des rattachements jugés faux** | **LIVRÉ, OBSERVÉ** (22/08, `retrait_rattachements.py` + 3 boutons `/reglages`) | Le seul geste du projet qui **efface** une décision humaine au lieu de la déplacer — d'où trois garde-fous écrits dans la règle plutôt que dans le bouton : rien ne part sans un verdict EXPLICITE sur CE couple (un cas non jugé ou « indécidable » ne produit aucun geste) ; seul `faces` bouge (`exclude` et `confirmed` sont keyés par PHOTO, aucun index ne peut y glisser) ; et deux applications de suite donnent le même état (un couple déjà parti se compte, il ne fait pas échouer). Les visages reconnus mais NON rattachés sont comptés à part et **jamais touchés** : ajouter un nom est une attribution, et la glisser dans un bouton nommé « retirer » poserait un nom en douce. Règle PURE, importée par le serveur ET par le banc — l'aperçu et l'application sont le même appel. Observé : **2 à retirer, 45 confirmés**, mêmes nombres par les deux chemins. |
| **Deux frères, et c'est la règle qui avait raison de refuser** | **OBSERVÉ** (22/08) | Les 2 seuls retraits sont **Res Jordi** sur `Bei Michael Jordi.jpg` et `Bei Michael Jordi2.jpg` : Mike avait confondu Res avec son frère **Michael**. C'est exactement le cas que `recale_rattachements` refuse d'arbitrer (« deux visages proches — fratrie, même personne détectée deux fois — ne prouvent rien, et trancher au hasard serait pire »). Une fratrie ne se tranche ni au score ni à la géométrie : seulement par quelqu'un qui les connaît. Et la première réponse de Mike était fausse : c'est sa SECONDE passe, sur la page qui montre la scène entière, qui a corrigé. |
| **Le retrait : APPLIQUÉ et OBSERVÉ** | **CLOS** (22/08, geste de Mike) | **2 rattachements retirés**, couples **1 194 → 1 192** — exactement les deux, rien d'autre n'a bougé. Quarantaine `_corbeille_retraits/` écrite AVANT. Un second aperçu les compte « déjà absents » : le geste est **idempotent**, et le message le DIT (« Déjà fait » et non « 2 à retirer », qui se lirait comme du travail restant). Le chantier des rattachements est clos : 42 décalés au départ, 33 recalages dont 29 vraies réparations, 28 cas jugés à l'œil, 2 retraits. Reste 9 « décalés » qui sont des apparitions multiples (pages d'album, montages), 13 « faux positifs » jugés JUSTES à 12 sur 13, et 155 photos à un seul visage. |
| MegaDescriptor plutôt que DINOv2 | **REJETÉ** (31/07) | DINOv2 garde +3,4 pts r1. |
| `sqlite-vec` ; embeddings INT8 | **REJETÉ** (11/08) | Cosinus numpy sur BLOB suffit ; INT8 recall@10 0,9685. |

| **La sauvegarde oubliait 20 Mo sans lesquels un PC neuf ne repart pas** | **MESURÉ, CORRIGÉ** (22/08, `verifier_restauration.py`) | `backup_db()` ne poussait que `photos.db` et le journal des jugements. L'instrument a nommé **9 artefacts IRRÉCUPÉRABLES présents ici et nulle part ailleurs, 19,9 Mo** : `docs/undo_*.json` (7,3 Mo — la carte des 19 331 déplacements, devenue porteuse le jour même), les trois quarantaines (`_corbeille_detections`, `_corbeille_vecteurs`, `_corbeille_decisions` — sans elles les purges cessent d'être réversibles) et six fichiers de réglages saisis à la main, dont `dossiers_a_taguer.txt` sans lequel le serveur ne voit plus aucune photo. `backup_artefacts()` les copie, incrémental — **observé** : 61 fichiers, 20,4 Mo à la première sauvegarde, et l'instrument relancé annonce « Total exposé : 0 o ». Et l'instrument CONSTATE la copie dans le dossier de sauvegarde au lieu de l'affirmer — c'est le piège que `backup_verify` avait déjà fermé une fois. |
| **Le plafond de 1 500 se lisait comme une exhaustivité** | **CORRIGÉ, OBSERVÉ** (22/08) | `espece:chat` rend **2 386** photos, la page en affichait **1 500** sans un mot : 886 disparues en silence. Le filtre déterministe connaît son total AVANT de couper (`detail['total']`, `detail['tronque']`) ; la page dit « 886 de plus non affichées (sur 2386 au total) ». Le plafond lui-même reste à 1 500 : c'est le rendu qu'il protège, pas le filtre. |
| **`/pets` parlait de « chats » pour six espèces** | **CORRIGÉ** (22/08) | Le pipeline reconnaît chat, chien, oiseau, cheval, vache, mouton — la page disait « Aucun chat nommé », « chats détectés », « Retirer de ce chat ». Libellés alignés sur ce que l'utilisateur contrôle (plancher `photo-ui` : nommer par la chose, pas par la mécanique). |
| **Le bandeau d'analyse IA se taisait pour de bon** | **CORRIGÉ** (22/08) | `poll()` ne se re-programmait QUE tant que la file n'était pas vide : une fois à zéro, plus aucun sondage — un envoi depuis le téléphone n'allumait plus rien jusqu'au rechargement. Il ralentit (60 s) au lieu de s'arrêter, et il s'ANNONCE (`role="status"`, `aria-live="polite"`). |
| **Deux tableaux de mois recopiés avaient divergé** | **CORRIGÉ** (22/08) | Le bouton disait « Meme jour (14 aout) », la page d'arrivée « 14 août ». Le tableau des mois vient maintenant du serveur (`meme_jour.MOIS_FR`) par un marqueur de gabarit : une seule source, et le test des gabarits la contrôle. |

| **`PETS` mesuré pour la première fois : l'index est SAIN** | **MESURÉ — pas de campagne de recalage** (22/08, `mesure_rattachements_animaux.py`) | 12 fiches, **351 couples**, 330 mesurables. **0 hors bornes**, et **10 décalés dont 8 sur des photos que la fiche cite plusieurs fois** (Mutz cité 7× sur `111-1103_IMG.JPG`, 10 animaux) — le nommage d'un groupe, pas un index qui glisse. Restent **2 vrais candidats sur 330 (0,6 %)**, contre 3,5 % côté visages avant réparation. La raison est dans le code : rien ne ré-embarque une photo déjà connue côté animaux (`animal_worker` saute `ANIMAL_STORE.has`), et `migrate_animal_pipeline` vide TOUT et remet `faces = []`. **Porter `recale_rattachements` aux animaux est REJETÉ** : la population traitée serait de 2 couples. Deux tas précis restent, pour un geste humain : **15 clés mortes** (Inti 7, Luna 5, Pins 2, Pticon 1) et **6 couples d'espèce incohérente** (Luna, chat, sur une détection `dog`, 4 photos). Le repli silencieux existe pourtant aussi ici — `_serve_animalcrop` sert l'animal 0 hors bornes, comme `_serve_facecrop`. |
| **L'empreinte DINOv2 ne reconnaît pas 37 % de ses propres rattachements** | **MESURÉ — ne rien régler sans jugement** (22/08) | Sur 330 couples **confirmés**, **122 scorent sous `PET_MATCH_SIM = 0,55`**. Médiane **0,603**, p10 **0,392**, min **0,231**, max 0,989 : le seuil coupe au MILIEU de la distribution des rattachements justes. La même colonne vaut **1,1 %** côté visages. Un seuil bas nomme une cécité, pas une faute (22/08) — mais ici la cécité est massive, et c'est elle qui limite tout ce qu'on voudrait automatiser sur les animaux. Ni monter ni baisser le seuil ne se décide sur ce chiffre seul : il faudra des jugements, comme pour la tranche 0,35–0,40. Corroborations au passage : **toutes** les empreintes sont en 768 (la protection de dimension n'a rien à attraper), et **4 628 détections sur 7 704** en portent une — l'écart de 3 076 est **exactement** bird 1 729 + sheep 710 + cow 637, les espèces non nommables. Et **651 photos portent un nom d'animal sans aucun rattachement** (Inti 420, Mutz 111, Luna 94 ; Puma, Kevin et Le chat de Bremblens en ont 7, 6 et 2 pour zéro couple) : c'est la réserve, pas un défaut. |
| **H4 — « l'espèce tranche sans seuil » : RÉFUTÉE** | **6 sur 6, REGARDÉES** (22/08) | Le banc des animaux tenait l'espèce pour son verdict le plus solide : une détection `dog` sur la fiche d'un chat est fausse, aucun score n'a besoin de le dire. Le score de la détection DÉSIGNÉE, ajouté ensuite, a dit l'inverse — 0,441 / 0,594 / 0,604 / 0,623 / 0,666 contre une médiane de **0,603** sur les couples confirmés. Les six crops ouverts dans `/api/animalcrop` : **six chats crème**, dont un vu deux fois sous deux chemins. **C'est l'ÉTIQUETTE d'espèce de YOLO qui ment, pas le rattachement**, et l'erreur est VISIBLE : ces photos rangent Luna sous « chien » dans l'axe espèce. Deux leçons de plus : un couple à **0,441** s'est révélé juste — un seuil bas nomme une cécité, une fois de plus ; et le seul « recalage évident », i=0 → i=1 à **+0,036**, était deux BOÎTES du même chat — recaler aurait été un rebrassage. Un score parfait est une alarme, y compris quand c'est le sien. |
| **Les 15 clés mortes des animaux : aucune contrepartie, GARDÉES** | **MESURÉ, CLOS** (22/08, `--a-juger`) | Trois chemins, trois fois rien : les journaux d'annulation (19 331 déplacements) n'en connaissent aucune ; aucune clé VIVANTE ne porte le même nom de fichier ; et le DISQUE tranche — `verifier_orphelins --filtre ARZOPA --table animals` rend **115 entrées, 0 présente, 115 « disparu »**, dont 12 jugées par un humain. Ces photos n'existent plus nulle part : ni re-clé, ni recalage n'est possible. Suite du choix du 22/08 sur le résidu des visages, on **garde** — purger est irréversible, et leurs détections survivent sous l'ancien chemin, donc un humain peut encore les regarder. |
| **`-stay_open` pour l'écriture XMP : 25 %, pas 12×** | **MESURÉ — PARQUÉ après le reste** (23/08, `mesure_xmp_debit.py`) | Trois régimes sur 30 photos du fonds, en LECTURE : un processus par photo **0,80 s**, un seul processus pour tout le lot 0,23 s, `-stay_open` **0,07 s** — 12×. Ce 12× ne se transpose PAS à la file : il isole le seul terme que `-stay_open` supprime, le **démarrage du processus (0,74 s)**. Une ÉCRITURE coûte **2,91 s/op** (mesuré sur 7 172 s de file vivante) — le reste, c'est la réécriture du fichier sur SMB. Gain réel **2,91 → 2,17 s/op, 25 %**, contre un facteur **2** pour le simple groupement des deux gestes d'une photo, livré le même jour. Un processus qui vit longtemps et tient le NAS ne se paie pas 25 % : à faire après le reste. **Et un piège nommé** : `-q` emporte le `{ready}` de `-stay_open` — le banc a attendu 600 s, la fenêtre des bancs avec lui. Depuis, chaque ordre a un délai : un banc doit ÉCHOUER, jamais se figer. |

| **Deux jugements contradictoires : le dernier gagne, mais AUCUN ne s'efface** | **REMPLACÉE le 29/08** (adoptée le 28/08) | *Remplacée par « le PROPRIÉTAIRE de la photo l'emporte » (ligne ci-dessous, choix de Mike le 29/08, re-confirmé quand la contradiction entre les deux lignes a été vue) : ce qui reste vrai — rien ne s'efface, la fiche dit le désaccord. Ce qui change — QUI s'affiche : le propriétaire, pas le dernier.*  Florine dit « ce visage est Ellie », quelqu'un dit « non ». Jusqu'ici la dernière écriture gagnait EN SILENCE — sans conséquence à un seul utilisateur, règle manquante à vingt. Désormais : le dernier jugement fait foi pour l'affichage, **les deux sont conservés**, et la fiche DIT qu'il y a désaccord ; Mike seul tranche. C'est la règle 2 du projet (« les noms humains ne se perdent jamais ») étendue à « les noms de QUI » : une décision humaine ne peut pas être écrasée par une autre sans que ça se voie. Coût : du stockage. Bénéfice : le désaccord devient une donnée, pas une perte. |
| **Un nom devient COMMUN dès qu'il touche un deuxième propriétaire** | **ADOPTÉ** (28/08, choix de Mike) | Cas limite du renommage par un non-admin : Florine nomme quelqu'un sur SES photos, puis cette personne apparaît sur celles de Mike. Le basculement est **automatique** — dès que le nom touche les photos d'un deuxième propriétaire, il cesse d'être privé et son renommage repasse par l'admin — et il **se dit dans la fiche**, pour que la perte du droit de renommer ne soit jamais une surprise. Alternative rejetée : réserver tout renommage à l'admin dès le premier jour, qui rendrait une bibliothèque personnelle inutilisable sans lui. |
| **L'upload devient la boîte de réception du PROPRIÉTAIRE : `Photos <Nom>\_A TRIER\`** | **ADOPTÉ** (29/08, choix de Mike) | `_Uploads` était le début du projet : une racine « plate » à part (clés = nom nu, `scan_uploads`, `key_for_new_path`), cible unique du bouton du téléphone. Avec le multi-utilisateur, ce qu'un utilisateur envoie tombe dans SA boîte de réception, `Photos <Nom>\_A TRIER\`, et le même plan par année le range dans `Photos <Nom>\<année>` — une seule règle pour le téléphone, le Takeout et le dépôt à la main ; le propriétaire est donné par le dossier, comme pour le reste du chantier 17. `_Uploads` se vide par ce plan et disparaît, avec sa clé « nom nu ». Alternative rejetée : un `_Uploads <Nom>` par utilisateur, qui aurait gardé une racine spéciale de plus à re-cler. |
| **L'auteur d'une décision vit dans un dictionnaire `auteurs` de la fiche, à côté des listes** | **ADOPTÉ** (29/08, choix de Mike) | Une décision humaine est un élément de liste (`faces` = `[clé, idx]`, `exclude`/`confirmed` = clés) sans place pour un nom, et tout le re-clé, l'undo et les bancs lisent ces listes. On ne change PAS leur forme : la fiche gagne `auteurs` `{ "faces:<clé>:<idx>" \| "exclude:<clé>" \| "confirmed:<clé>" : "Mike" }`, migré en une passe (`migrate_auteurs()` : toutes les décisions existantes → Mike, journalisé, réversible) et re-clé par la même primitive que les listes. Alternative rejetée : des triplets `[clé, idx, auteur]`, qui auraient cassé tous les lecteurs d'un coup. |
| **Deux jugements contradictoires : le PROPRIÉTAIRE de la photo l'emporte sur ses photos, l'admin ailleurs** | **ADOPTÉ** (29/08, choix de Mike) | Florine exclut, Mike confirme : sur une photo de `Photos Flo`, c'est Florine qui a raison ; hors d'un dossier propriétaire, l'admin arbitre. La décision perdante n'est pas effacée : elle reste dans `auteurs` avec le suffixe `#contesté`, visible dans la fiche, pour qu'une perte de jugement ne soit jamais silencieuse. Alternative rejetée : « le dernier qui écrit gagne », qui fait perdre un jugement sans le dire. |
| **Verser le Takeout dans `_A TRIER` pour qu'il passe au NAS** | **REJETÉ, MESURÉ** (08/09, question de Mike) | Le Takeout a DÉJÀ été confronté au fonds, fichier par fichier, par `verifier_photos_google.py` : **13 905 médias comparés, et le rapport final du 28/08 ne porte plus de clé `ABSENT` du tout** — elle valait 3 776 le 27/08, avant que le bat 32 ne les copie. Tout ce que le Takeout contient est au NAS. Et sur les 25 864 fichiers de l'extrait, **11 959 sont des `.json`** — la moitié du compte est de la métadonnée que la photothèque ignore ; le reste est 10 608 `.jpg` (37,2 Go) et 3 114 `.mp4` (58,5 Go), soit 13 891 médias, ce qui recoupe les 13 905 comparés. **Ce que ça coûterait** : +96 Go au NAS, +13 891 entrées dans le scan ET dans la campagne de retag (qui en a 27 269 devant elle — elle grossirait de moitié), puis un dédoublonnage pour tout retirer — le bat 40 en a déjà retiré 2 929 pour 10,45 Go une fois. **Et le pire** : les 9 625 « PROBABLE » sont des ré-encodages Google en mode économiseur, donc PLUS PETITS que l'original du NAS ; les verser reviendrait à importer des copies dégradées de photos qu'on a en mieux. Le Takeout reste une SAUVEGARDE, pas une source. |
| **L'AUTRE moitié du garde-fou NAS : le scan cède à une étape lourde déjà partie** | **ADOPTÉ, BANC VERT, OBSERVÉ EN NON-RÉGRESSION** (08/09) | Le 06/09, deux balayages SMB concurrents ont coûté 85 minutes au lieu de 632 s. Le correctif d'alors ne fermait qu'un sens : `sv.is_busy()` empêche une étape lourde de DÉMARRER pendant un scan. **Une fois l'étape lancée, plus rien ne parlait au scan** — un tour NAS tombant cinq minutes après le départ d'un `recensement` rejouait la même panne, à l'envers. Deux crochets (`etape_lourde_debut`/`fin`, posés autour des seules étapes `LOURDES`, levés dans un `finally`) et la boucle de scan qui les consulte. **Trois choix, chacun contre une panne connue** : (a) elle REPORTE, elle ne bloque pas — bloquer ferait attendre la file de retag derrière un recensement de vingt minutes ; (b) l'échéance n'est pas PERDUE (`nas_reporte` la rejoue au tour suivant), sinon un `deep` tombé pendant un recensement sauterait son lot de retag en silence ; (c) **plafond de 3 reports** — sans lui, une maintenance qui n'en finit pas affamerait le scan, exactement le défaut qu'on répare, retourné. Le `getattr` côté `maintenance.py` est pour les services de banc ; un banc EXIGE les deux méthodes de `_MaintSv`, sinon l'optionalité cacherait l'absence là où elle compte. **Ce qui n'est PAS prouvé, et il faut le dire** : la collision elle-même n'a pas été observée — elle demande qu'une étape lourde et un tour NAS se croisent. Le banc tient la mécanique, le redémarrage montre la non-régression (scan de démarrage normal, zéro report journalisé, campagne reprise en 4 s). |
| **Les Motion Photos : la vidéo ne revient JAMAIS, même quand elle est la dernière copie** | **TRANCHÉ PAR MIKE** (09/09) | Le 09/09, le bat 33 proposait **1 913 fichiers à rapatrier depuis le Takeout — dont 1 814 étaient les vidéos embarquées** que le strip du 03/09 avait retirées. Les copier aurait DÉFAIT ce travail. Mike : « *les images avec 1-2 secondes de mouvement sont inutiles et prennent de l'espace disque pour rien… c'était un réglage involontaire sur mon téléphone, une seule image me suffit.* » **Ce n'est donc pas un arbitrage au cas par cas, c'est une règle** : une Motion Photo ne garde que son image, et sa vidéo ne se rapatrie pas, même quand Google en est le dernier détenteur au monde et que l'effacement est irréversible. **Conséquence assumée** : au bat 49, ~2 400 vidéos disparaissent définitivement. **Et le vrai correctif est en AMONT** — tant que le réglage « Photo animée » du téléphone est actif, chaque nouvelle photo arrive avec sa vidéo et le bat 42 devra repasser. Couper le réglage règle le problème à la source ; le bat 42 ne fait que ramasser derrière. |
| **Les 14 fichiers SANS EXTENSION : les vidéos des Motion Photos, effacées** | **IDENTIFIÉ, TRANCHÉ** (09/09) | Ils dormaient dans `_A TRIER\Takeout Google`, 2 à 11 Ko, sans extension, et le rangement ne savait pas les classer. **Deux preuves indépendantes** : ce sont de vrais MP4 complets (`ftyp` + `mdat` en taille 64 bits + `moov` présent dans les 14 — donc PAS tronqués, contrairement à ma première lecture, qui venait d'un analyseur ne gérant pas la taille étendue) ; et **les 14 noms sont dans le manifeste du strip, 14 sur 14**. Google sépare une Motion Photo en JPG + MP4 à l'export ; ce MP4 séparé n'a jamais existé au NAS, donc il est sorti ABSENT et le bat 32 l'a rapatrié en toute logique. **L'absence d'extension les a SAUVÉS de se disperser** dans `Photos Mike\2024|2025` — le rangement les a bloqués faute de savoir les classer. Effacés par Mike. *Deux hypothèses fausses avant celle-là : « des `.json` compagnons » et « des vidéos tronquées ». Aucune des deux ne coûtait rien à vérifier.* |
| **Le bat 32 rapatrie tout ce qui est ABSENT — y compris ce que les règles produit REFUSENT** | **CONSTATÉ, à corriger** (09/09) | Sur les **570 médias** copiés le 08/09 : **394 captures d'écran** (que « des souvenirs, pas des documents » rejette) et **14 vidéos de Motion Photo** (que la règle du 09/09 rejette) — soit **408 sur 570, 72 %**, que les règles de Mike refusent. Rien n'est perdu (les captures tomberont dans l'onglet Sensibles, un clic chacune), mais c'est du scan, du tagging et du tri pour rien. **Le bat 33 a appris à filtrer par son ENTRÉE** (`_google_a_rapatrier.json`) le même jour ; le bat 32 devrait faire pareil. Non fait : la chaîne était trop avancée pour la reprendre, et le coût réel était 0,99 Go. À faire au prochain export. |
| **Effacer l'extrait du Takeout (95,78 Go)** | **BLOQUÉ par la preuve** (08/09, demandé par Mike) | La règle est écrite dans l'instrument : « ABSENT : le NAS ne connaît pas ce nom. **Rien ne s'efface tant qu'il en reste.** » Preuve REFAITE au lieu d'être citée : **ABSENT 570** aujourd'hui contre **0 le 28/08** — le fonds a bougé entre-temps (dédoublonnage par l'image 2 929 fichiers, strip des Motion Photos, coquilles en quarantaine). **Un rapport de onze jours n'est pas une preuve d'aujourd'hui**, et c'est exactement ce que j'allais faire une heure plus tôt en disant l'effacement sûr. Les 570 pèsent 0,99 Go : 394 captures d'écran, **169 vraies photos de personnes** (`Moi (34).jpg`, `Sandra (56).jpeg`, 2014-2021), 7 divers. L'ordre : bat 32, puis re-vérifier jusqu'à ABSENT 0, puis effacer. |
| **Les noms en `(N)` du Takeout sont des suffixes de collision** | **RÉFUTÉ** (08/09) | Hypothèse plausible — le Takeout suffixe bien les collisions — et **fausse ici : 7 sur 160**. Le `(N)` des autres est la NUMÉROTATION D'ALBUM de Google (« Moi », « Val », « Sandra », « Alba »). Testée en une ligne : retirer le suffixe et chercher le nom nu parmi les CERTAIN/PROBABLE du même rapport. **Une hypothèse qui se teste pour rien se teste**, surtout quand elle sert à justifier un effacement. |
| **Chercher les 942 « contenu perdu » dans le Takeout** | **SANS OBJET — c'était déjà fait** (08/09) | Je l'ai porté deux fois dans `ROADMAP.md` comme une tâche ouverte, après avoir lu « à rapatrier AVANT de toucher au Takeout » trois lignes au-dessus d'un « FAIT PAR MIKE le 06/09 ». **Une consigne de prudence n'est pas une tâche.** La contre-mesure a quand même été écrite (`verifier_perdus_dans_takeout.py`) parce qu'elle refait la question par un AUTRE chemin — les coquilles lues sur le disque, l'extrait marché directement — et elle **corrobore** : 938 coquilles, **932 absentes du Takeout, 6 présentes**, et ces 6 ont déjà leur vrai jumeau au NAS. Rien à récupérer. |
| **Trier les coquilles de récupération de disque par leur TAILLE** | **RÉFUTÉ, PROUVÉ** (08/09) | Les 6 copies Google pèsent **exactement le même nombre d'octets** que les coquilles, et leur en-tête est `ff d8 ff e1` — de vrais JPEG. L'outil de récupération a donc écrit des fichiers **de la taille exacte des originaux**. C'est la preuve de ce que le 05/09 constatait sans l'expliquer (« une règle de taille minimale n'en aurait écarté AUCUN »). Le bon axe est l'EN-TÊTE, pas le poids — et mon propre banc, qui classait ces 6 en « douteux » sur le seul motif du poids, a dû être corrigé pour sonder les deux premiers octets. |
| **Mettre la sauvegarde Takeout sur le NAS, hors du fonds** | **PROPOSÉ** (08/09) | L'instinct de Mike — « le NAS est le bon endroit » — est juste, c'est le chemin qui ne l'était pas. C: a 1 To, le NAS **2 980 Go libres et de la redondance**. Un dossier dont un composant commence par `.`, `@` ou `#` est invisible à la photothèque (`_is_hidden_path`, vérifié dans le code) : `\\NAS-Bremblens\home\Photos\.sauvegarde-takeout\` serait sur le NAS, sauvegardé par lui, et jamais scanné ni tagué. Ce n'est PAS la copie hors site du chantier 12 bis — un NAS chez soi ne protège ni du feu ni du vol — mais c'est mieux que sur le disque système du PC. |
| **Les 116 paires Google/NAS indéterminées : closes sans action** | **TRANCHÉ PAR MIKE** (29/08) | ABSENT = 0 est acquis ; les 95 incomparables (74 trailers contenant eux-mêmes un `FF D9`, 21 non-JPEG d'un côté) et les 21 vraiment différentes (listées dans `_reprise_google.json`) restent telles quelles sur le NAS, et le Takeout dézippé garde la version Google jusqu'à la copie hors site. Alternatives rejetées : revue à l'œil des 21 (rien n'en dépend), rapatriement des 21 versions Google (des doublons volontaires de plus à revoir). |
| **Les 9 ré-encodages Google homonymes du fonds (`Google porte mieux`) : quarantaine, le NAS reste** | **TRANCHÉ PAR MIKE** (29/08, bat 37) | Bat 26 les sautait à vie (collision) et le plan disait « 9 à ranger » pour toujours. Mesuré avant le geste : hachage d'image différent (re-encodage), **mêmes noms** sur les 9 paires (`/api/faits`), et le NAS porte un **GPS que Google a retiré** (La Paz, Sucre, Prilly, Rolle, Bremblens). Remplacer le fichier du fonds aurait perdu le lieu ; garder les deux aurait semé 9 quasi-doublons. Le retrait n'est PAS un doublon au sens du hachage : il passe par une option explicite (`--homonymes-differents`), jamais par défaut, et saute toute copie qui porterait un nom absent de l'homonyme. Réversible (corbeille, undo). |
| **Les vidéos de `_A TRIER` se rangent comme les photos : `Photos Mike\<année>`, y compris le dump « Samsung Floflo »** | **TRANCHÉ PAR MIKE** (29/08, bat 39) | Règle 17c : ce qui sort de `Photos\_A TRIER` appartient au propriétaire du fonds. Les 5 vidéos WhatsApp du dossier « 180328 Samsung Floflo » sont un dump du téléphone de Mike, comme les photos du même lot ; si elles sont à Flo, elles passeront par `Photos Flo\_A TRIER` (boîte de réception par propriétaire) et bat 39 les rangera chez elle. La date d'une vidéo vient du NOM, sinon d'ExifTool, sinon du dossier année — jamais du mtime (3 073 vidéos, 0 sans date). |
| **Les 125 `*.jpg_original` de `_A TRIER` : GARDÉS jusqu'à la vérification des stills, puis purge par un bat dédié** | **TRANCHÉ PAR MIKE** (29/08) | Ce sont les versions complètes (trailer Motion Photo) laissées par le strip — l'étape (1) de la règle 1 septies ; leur purge est l'étape (2), après avoir regardé une poignée de stills. `inventaire_dossiers_vides` les compte RELIQUATS et bat 38 ne les touche jamais ; un dossier qui n'a plus qu'eux n'est pas « vide ». |
| **Le trailer Samsung (SEF) : question SANS OBJET** | **TRANCHÉ PAR MIKE** (29/08) | La règle Motion Photo (« une Motion Photo ne garde que son image », `exiftool -trailer:all=`) jette le trailer entier de toute façon : savoir si notre écriture XMP l'abîmait ne changerait aucun geste. La preuve de cause armée (`_rapport_sef_avant.json`, hors git) n'est plus attendue ; le fichier part avec le strip. Mesure conservée pour mémoire : nommées 86,9 % avec SEF vs non nommées 83,9 % — rien n'accusait l'écriture XMP. |
| **Un rattrapage ne détruit jamais plus que ce qu'il répare : sur un EXIF illisible, on écrit XMP + IPTC SANS toucher l'EXIF** | **MESURÉ, ADOPTÉ** (29/08, `verifier_reparation_exif.py`) | Sur un Motion Photo Samsung, ExifTool refuse la voie MWG (« Error reading OtherImageStart data in IFD0 »). `repair_file` (`-all=` puis recopie) écrivait les tags mais jetait le trailer — la vidéo embarquée, 2 à 3 Mo — et le profil ICC, même avec `--trailer:all` ; 14 photos de 2024 l'ont subi le 28/08 (le `_original` porte ce qui manque). Mesuré sur une copie : la voie XMP + IPTC seule écrit les tags, garde vidéo, trailer et ICC (+501 octets). `write_metadata` la tente d'abord ; `repair_file` n'est plus qu'un dernier recours, et le dit. |

## Tagging / description

> Sorti le 08/09 dans **`eval/DECISIONS_TAGGING.md`** (deuxième
> découpage par DOMAINE, choix de Mike, le même jour que le
> budget à 125 000). Modèles, prompts, vocabulaire, campagnes de
> re-tagging, dictionnaire FR→EN, endurance thermique.
> Ce que la photothèque FAIT des tags — recherche, filtres, axes —
> reste ici.

## Renommage

| Idée / piste | Verdict | Raison |
|---|---|---|
| Appliquer les 7 058 renommages | **FAIT** (17/08) | 36 lots, **0 sauté**, undo complet. |
| Chercher les −250 sans instrument | **REJETÉ → instrument OBSERVÉ** (18/08) | Rien n'était perdu, rien ne comptait : `comptes_index.py` compte au GOULOT. |
| Le repli sur le NOM gardé comme `taken` | **CORRIGÉ** (19/08) | Le scanner écrit le scan dans `DateTimeOriginal` **et** dans le nom : le garde-fou du 17/08 ne fermait qu'une porte sur deux. |
| Le plan revient sur ce qu'il a renommé | **CORRIGÉ** (19/08) | `est_nom_annee_seule` n'agit que si la date est devenue précise (plan à **0**). |

| **`is_file()` avant d'enfiler une écriture XMP** | **RETIRÉ, et c'était une FUITE** (23/08) | `_enqueue_person_write` testait l'existence du fichier et, sur un « non », ne notait rien, n'enfilait rien, ne disait rien ; `_file_personnes_reprise` faisait pareil au démarrage — le pire moment, le NAS n'étant pas toujours monté : la prudence de la reprise annulait le journal qu'elle devait sauver. Or `is_file()` interroge un partage SMB, qui répond « non » sur un fichier qui existe. Le compte : **18,7 % des couples nom–photo** (Wilson 16,7–20,9 %, 255 écarts sur 1 364 lus, 352 noms) — soit **~5 800 photos** dont l'index porte un nom que le fichier ignore. Les deux seuls noms sans écart, Florine (200/200) et Stéphane Plouvin (58/58), sont les deux dont les fichiers ont été RÉÉCRITS en entier. Désormais : on note, on enfile, et le seul qui déclare une écriture impossible est celui qui l'a TENTÉE (`_file_personnes_echecs.jsonl`). **4 rouges observés sur l'ancien code**, dont deux tests d'hier qui affirmaient l'inverse. |

## Lieux · recherche (chantier 14a)

| Idée / piste | Verdict | Raison |
|---|---|---|
| **Un jeton `<axe>:<valeur>` qu'on ne sait pas satisfaire rend RIEN, et le DIT — sur les CINQ axes** | **ADOPTÉ, OBSERVÉ** (26/08, `recherche.extraire_jetons`) | `/files?q=animal:Zzzznexistepas` rendait **1 500 photos** annoncées comme un FILTRE : le jeton ne ressemblait à aucun nom NU (`_extraire_noms` ne connaît aucun préfixe), il partait donc en sémantique — qui classe tout le fonds et ne rend jamais vide. Défaut corrigé le 21/08 pour `espece:licorne` et oublié sur les quatre autres axes, **alors que l'interface écrit elle-même ce vocabulaire** (« y chercher personne:Luna a du sens », `gallery.html`). Trois règles communes : valeur inconnue → rien + on nomme la valeur ; AXE inconnu → rien + on nomme l'axe ; jamais de repli silencieux sur le sens. `extraire_especes` n'est plus qu'une VUE sur la primitive commune : la règle n'a plus qu'un seul endroit où être fausse. Observé sur le serveur vivant : **8 griefs puis 15/15 verts**, `animal:Caline` 730 photos comme le nom nu, `lieu:Achumani` 198, `couleur:rouge` 0 et nommé. |
| **Le bon AXE est exigé pour le bon nom** | **ADOPTÉ** (26/08) | « Luna » est une chatte : `personne:Luna` ne rend pas ses photos, il dit qu'aucune PERSONNE ne porte ce nom. Rendre l'animal serait deviner à la place de l'utilisateur ; les rendre TOUTES était le défaut d'hier. |
| **Un extracteur de jetons ne juge que SON axe** | **ROUGE OBSERVÉ, CORRIGÉ le jour même** (26/08) | Première version : `extraire_especes`, devenu générique, annonçait « espèce inconnue : Caline » sur `animal:Caline` et **mangeait le jeton** — huit griefs d'un coup au premier passage du banc. `axe_inconnu_refuse` est donc à False par défaut : seul le DERNIER extracteur, celui qui connaît tous les axes, refuse ce qui reste. Le banc a payé son écriture dès sa première exécution. |
| **Un espace avant le deux-points n'est pas un filtre** | **ADOPTÉ** (26/08) | « Luna : la chatte » et « 18:30 » traversent intacts. Le garde-fou n'exige le deux-points COLLÉ que pour un axe INCONNU ; sur un axe connu la graphie espacée reste acceptée (`espece : chats`, 20/08). Un garde-fou qui refuse une phrase ponctuée coûte plus qu'il ne rapporte — deux contrôles de FAUX POSITIF le tiennent. |
| **Le contrôle NÉGATIF est un banc, pas une habitude** | **ADOPTÉ** (26/08, `verifier_filtre_negatif.py`) | 15 contrôles sur 5 axes, dans les DEUX sens : une valeur inventée rend zéro **et** le dit ; une valeur réelle rend au moins une photo — **sans ce second sens, un moteur qui rendrait zéro pour tout serait vert**, le mode de panne des deux bancs déjà démasqués ici. Les valeurs positives sont lues dans le fonds (`/api/names`, `lieux.txt`), jamais en dur : un banc qui teste « Luna » sur un fonds sans Luna rougit sans qu'aucun code n'ait bougé. Il contrôle les DEUX canaux — `/api/search` et la page `/files?q=`, où le mensonge avait été vu — et refuse de conclure si `code_a_jour` est faux. |
| **« PROBABLE = Google a ré-encodé en mode économiseur »** | **FAUX, MESURÉ** (27/08, `verifier_google_pixels.py`) | Sur les 9 017 PROBABLE du vrai export : le NAS est plus gros **8 741 fois sur 9 017**, ratio médian **1,001**, et **uniquement sur les JPEG** — les 1 087 `.mp4` tombent exacts au bit près. Un ré-encodage divise le poids ; ici l'écart est de **+4,2 Ko médian, toujours du même côté**. C'est la métadonnée que la photothèque écrit elle-même dans SES fichiers (exiftool, XMP, images seulement). Compté et non supposé : **8 802 sont la MÊME image** — mêmes tables de quantification, mêmes tables de Huffman, même cadre, même longueur de flux, segments `APPn`/`COM` sautés. |
| **Un `sha256` du fichier pour comparer deux photos** | **REJETÉ** (27/08) | Il dirait « différents » et n'apprendrait rien : on SAIT que les octets diffèrent, c'est le point de départ. Ce qu'il faut comparer est ce qui fait l'IMAGE — le flux compressé et ses tables — en sautant les segments où vit la métadonnée. La bonne question n'est pas « mêmes octets ? » mais « mêmes pixels ? ». |
| **Le TRAILER n'est pas l'image** | **ROUGE OBSERVÉ, CORRIGÉ** (27/08) | Première version : **173 paires « flux différent »**, et j'ai écrit qu'il s'agissait probablement de deux photos DIFFÉRENTES de même nom. Le chiffre disait le contraire — l'écart était **toujours du même signe**, médiane −2 046. Contrôle : **les 173 portent, côté Google, des octets APRÈS le `EOI`**, médiane 2 046, exactement l'écart. L'instrument comptait `taille − début du flux`, donc rangeait le trailer dans l'image. Corrigé : trailer mesuré à part, **99 des 173 sont la même image**. Les 74 restantes ont un trailer contenant lui-même un `FF D9` — la recherche à reculons y tombe et le verdict sort ROUGE : **l'erreur possible va du côté prudent**, et c'est écrit dans le code. |
| **Un trailer différent n'est pas VERT non plus** | **ADOPTÉ** (27/08) | Une « photo animée » de téléphone vit dans ces octets. Même image + trailer manquant = le NAS a perdu quelque chose : c'est nommé (`MEME_IMAGE_TRAILER`), compté, et laissé au jugement de Mike. Ni un écart d'image, ni un non-événement. |
| **Le contrôle de l'EXTRACTION est un banc, pas une habitude** | **ADOPTÉ** (27/08, `verifier_takeout_ouvert.py`) | « Extraction effectuée OK » n'est pas une preuve : le dézippage a UNE panne qui se lit comme un succès, le fichier écrit à moitié — bon nom, bon endroit, mauvaise taille. Le contrôle est le dézippage MOINS l'écriture (`dezipper_takeout.extraire(appliquer=False)`), donc la même traversée : le geste et sa preuve ne peuvent pas diverger. Observé : 45 lots, **25 864 fichiers, 0 absent, 0 tronqué**, et le bat lancé par Mike arrive au même compte par l'autre bout. |
| **Une série de lots qui n'annonce pas son total** | **PORTÉE DÉCLARÉE** (27/08) | Cet export se nomme `takeout-<date>-1-001.zip` : un trou au milieu se voit, **un lot manquant à la FIN est invisible aux noms seuls**. Le rapport dit le plus haut numéro vu et renvoie à ce que la page Google annonçait. Un banc qui ne SAIT pas ne rend pas vert en silence. |
| **Le trailer perdu est un bloc Samsung SEF, et le NAS l'a à ZÉRO sur les 99** | **IDENTIFIÉ** (27/08) | Lu dans les octets : `SEFH`/`SEFT`, `Image_UTC_Data`, `Color_Display_P3`, `MCC_Data`, `Camera_Capture_Mode_Info`, `PhotoEditor_Re_Edit_Data` (avec le chemin d'origine et les données de ré-édition). ~2 Ko. Ni pixels, ni vidéo : de la métadonnée constructeur, dont un horodatage UTC et de quoi défaire une retouche Samsung. **Côté NAS, les 99 ont un trailer de 0 octet** — quelque chose l'a coupé, et le seul écrivain de ces fichiers est la photothèque (exiftool, écriture des XMP). Perte réelle, petite, SILENCIEUSE, et probablement générale à tout ce que la photothèque tague. |
| **Rapatrier les 99 « trailer perdu » depuis Google** | **REJETÉ** (27/08) | Leur pixel est DÉJÀ sur le NAS : copier créerait 99 doublons, dans un projet qui a un chantier entier de dédoublonnage. Et écraser le fichier du NAS par celui de Google détruirait les XMP — donc les noms humains, donc la règle 2. La bonne action est en amont : que l'écriture XMP cesse de couper le trailer. Les 99 déjà coupées sont une perte acquise, à chiffrer une fois la cause corrigée. |
| **Les ABSENTES vont sous `_A TRIER`, par ANNÉE** | **ADOPTÉ** (27/08, `copier_absentes.py`, choix de Mike « laisser faire la magie ») | C'est là que la chaîne existante reprend : `26 - Ranger par annee.bat`, scan, tagging, visages. L'année vient du `.json` de Takeout (`photoTakenTime`), sinon du dossier `Photos from YYYY`, **jamais du `mtime`** — un fichier copié aujourd'hui a le `mtime` d'aujourd'hui, et tout l'export passerait pour 2026. Vérifié sur les 3 776 réelles : **une année pour chacune, aucune en `_SANS_DATE`, aucune collision de nom**. |
| **Écraser un fichier existant pendant la copie** | **REJETÉ, garde-fou** (27/08) | Même taille → sauté (reprenable) ; taille différente → nom suffixé ET nommé dans le rapport. Aucun octet du fonds existant ne se perd : la règle 2 appliquée à une copie. Et la cible doit être sous `_A TRIER` (`--hors-a-trier` pour lever) : écrire 12,6 Go au mauvais endroit d'un NAS ne se rattrape pas. |
| **Le correctif du re-clé hors ligne, OBSERVÉ EN RÉEL** | **27 décisions** (27/08) | Le rangement des 559 photos de `_A TRIER` : `{'ok': 539, 'skip': 20, 'decisions': 27}`. Vingt-sept rattachements, exclusions ou confirmations ont suivi leur photo là où l'ancien code les aurait décrochés en silence. Le premier lot de 20 avait rendu **zéro** — ces vingt-là n'en portaient aucune : un échantillon qu'on n'a pas choisi ne prouve rien, ni dans un sens ni dans l'autre. |
| **`appliquer_plan.rekey_stores` se disait « miroir » et re-cléait deux magasins sur quatre** | **CORRIGÉ** (27/08, `recler_decisions_humaines`) | Le correctif du 22/08 (`recle_decisions.recler_fiche`) n'avait été branché que dans `server.py`. Le rangement par année et le dédoublonnage décrochaient encore des décisions cinq jours plus tard, en silence — `store.rekey(chemin)` est un no-op sur `people`/`pets`, keyés par NOM, et la boucle ne regardait pas son retour. **Un docstring qui dit « miroir de » n'est pas une preuve** : c'est désormais un test qui le dit, et il ROUGIT sur l'ancien code. |
| **Demander « serveur arrêté » dans un en-tête** | **REJETÉ, remplacé par une PREUVE** (27/08) | `appliquer_plan_annee.py` le demandait depuis toujours et ne le vérifiait pas ; le 27/08 le serveur tournait. Le script refuse maintenant d'écrire, avec `--forcer` pour lever explicitement. |
| **Le `BEGIN IMMEDIATE` SEUL comme preuve d'arrêt** | **INSUFFISANT** (27/08) | La base est en **WAL** : un lecteur n'y bloque pas un écrivain, et le serveur ne tient le verrou d'écriture que pendant ses transactions — quelques millisecondes. `BEGIN IMMEDIATE` peut donc réussir alors que le serveur est vivant, et rendre un feu vert qui ne prouve rien. On interroge AUSSI `GET /api/serveur` : une preuve qui ne dépend pas de l'instant. Ni l'une ni l'autre n'est suffisante seule (un serveur figé ne répond pas non plus) — d'où les DEUX. |
| Gazetteer `cities1000` SEUL | **CORRIGÉ** (14/08) | Il s'arrête à 1 000 hab. : le domicile sortait « Bussigny ». |
| API de géocodage cloud (TomTom, OSM…) | **REJETÉ** | Vie privée du GPS familial ; clé/quota/réseau. |
| Chercher un lieu dans le SEUL chemin | **CORRIGÉ** (15/08) | 6 595 photos ont un `gps_place` que leur dossier ignore. |
| `_best_time` comme source d'année d'un filtre | **REJETÉ** (15/08) | Il retombe sur `mtime` : le tagging de 2026 a réécrit une photo de 1998. Précise, sinon DOSSIER. |
| **Backfill ÉCRIT du champ `faits`** | **REJETÉ** (19/08) | Un champ figé se périme : sur 81 pourvues, la VUE en corrige **4**. Gravé 43 064 fois pour rien. |
| **Segments entiers = la bonne règle pour ce qu'on VOIT** | **FAUX, et c'est le résultat du chantier** (19/08) | Sur 876 lieux « collés », ~330 sont VRAIS — « Yani2004 » (219), « AchumaniAlto » (48). D'où la découpe des mots sur les frontières de CASSE et de CHIFFRES : « Vallorbe » reste entier, « Cousins&Cousines » ne rend jamais « Ins ». |
| **Règle de lieu unifiée sur ses trois appelants** | **ADOPTÉ, OBSERVÉ** (19/08) | `places_list` et `_cles_du_lieu` délèguent à `faits_vue.lieux_du_chemin` (`tous=True` : une photo compte dans CHAQUE lieu). « Ins » **493 → 5** ; recherche **499 → 11 dont 0** de « Cousins&Cousines ». Gains : Sud France 315, San Borja 82, Vallée d'Aoste 81, Rurrenabaque 55. Page **2 119 → 1 539 ms**. |
| Le NOM DE FICHIER comme source de lieu | **ADOPTÉ, limite assumée** (19/08) | 132 paires dont **71 déjà couvertes par le GPS**. Sur les 61 restantes : **52 vraies** contre **9 fausses** qu'aucune règle syntaxique n'attrapera (« Grupo en la Laguna »). Un booléen à basculer. |
| « France & Belgique » : deux lieux ou aucun ? | **TRANCHÉ PAR LA RÈGLE** (19/08) | `tous=True` les rend tous deux : 574 et 157. |
| Règle de lieu dupliquée serveur / banc | **CORRIGÉ** (19/08) | Tout délègue à `faits_vue`, que les bancs IMPORTENT (« Bremblens » : 2 398, non 30 682). |
| Vecteurs des photos sorties de l'index | **TRAITÉ** (17/08) | 2 374 purgés, réversible. **0 muet sur 1 600 résultats**. |
| **Faits AFFICHÉS depuis la vue (planche + visionneuse)** | **ADOPTÉ, OBSERVÉ** (19/08) | Un seul producteur client (`faitsHtml`), un seul assembleur serveur (`faits_vue.assertions`), les quatre modes de `/files` partagés. `q=Ins` : 11 photos — 11 dates, 11 lieux, 5 noms ; page **1 048–1 462 ms** contre 1 539. `q=montagne` : 1 500 photos en **477–1 082 ms**. |
| **Index inversé des noms, en DEUX passes** | **ADOPTÉ, MESURÉ** (19/08) | Page de 50 : **1,11 ms** contre **9,65 ms** au balayage naïf (**×8,7**) ; index entier 2,234 s. Une passe unique est REJETÉE : `exclude` fait autorité partout, et un retrait posé par une fiche vue APRÈS celle qui attribue ne retirerait rien. Six tests. |
| Couverture « honnête » à 69,14 % | **PÉRIMÉ → 69,95 %** (19/08) | 30 122 photos avec un fait NON-date. Dénominateur du filtre, jamais les 99,79 %. |
| Le `mtime` comme date de repli pour CLASSER | **CORRIGÉ, OBSERVÉ** (19/08) | Le filtre le refusait, le tri le gardait : 257 photos datées de 2026 par leur tagging. |
| La page `/files?q=` se taisait | **CORRIGÉ, OBSERVÉ** (19/08) | `/api/search` disait ce qu'il avait compris ; la page, non. |
| « Fichier absent » comme seul critère de purge | **CORRIGÉ** (17/08) | « Il existe » ne dit pas « il sera re-tagué » : 91 photos hors racine. |
| **Le FILTRE des noms lit les `kw` bruts** | **CORRIGÉ, OBSERVÉ** (20/08, 14a-iv) | L'affichage lisait les fiches, le filtre lisait l'index : deux chemins pour une même question, donc deux réponses. **13 photos** que la recherche rendait alors qu'`exclude` avait retiré le nom (Mike 6, Flo 5, Silvio 1, Danica 1), **0** dans l'autre sens — les 363 tags nommés balayés sur copie. `_autorite_des_noms` : une implémentation, deux appelants. Observé après redémarrage : Silvio **495 → 494**, Danica **325 → 324**, les clés exclues absentes. |
| La FICHE fait foi sur l'ORTHOGRAPHE | **ADOPTÉ, OBSERVÉ** (20/08) | L'index porte des mots-clés écrits avant elle : « Luna · luna » sur 2 photos — le même chat nommé deux fois — et « luna » seul sur 1. Seule la graphie change, jamais le nom ; la recherche compare en minuscules. |
| **Portée du filtre déterministe : 92,74 %** | **MESURÉ** (20/08) | Sur les **30 122** photos à fait NON-date, nom ou lieu en atteint **27 936**. Les **2 186** autres n'ont qu'une ESPÈCE. |
| SigLIP retrouve ce que YOLO a vu | **FAUX, MESURÉ** (20/08, `mesure_espece_recherche.py`) | Rappel dans le top-1500 — ce que la page montre : chat 50,1 % (plafond de page 60 %), chien 50,3 %, oiseau 48,3 %, cheval 72,6 %, mouton 58,7 %, vache 52,2 %. Les 4 750 photos détectées ont TOUTES un vecteur : elles ne sont pas muettes, elles sont mal classées. |
| **`det_score` dit l'ESPÈCE** | **FAUX** (20/08) | Il dit « il y a un animal ici », pas laquelle. `chien` **0,866** sur `…chat-gris-et-blanc-allonge.jpg`, `cheval` **0,934** sur *chien, homme, barrière*, `mouton` **0,919** sur *alpaca, chèvre*, `vache` **0,808** sur *sculpture, fontaine*. Un seuil à 0,5 tenait sur deux échantillons de 12 — la table complète des six espèces l'a réfuté, et c'est Mike qui a lancé le banc. `sp_ia`, la vérification d'espèce, ne couvre que **766 détections sur 7 969**, et seulement le chat. |
| **La matière du 5ᵉ axe : la CONCORDANCE** | **ADOPTÉ** (20/08, choix de Mike) | Deux regards indépendants, YOLO et le tagueur (81 photos écartées : taguées AVEC les faits, elles récitent). Chat **2 316** (92,6 % d'accord), chien 356, oiseau 195, cheval 114, mouton 61, vache 43 — **3 065**. Le tagueur voit **760** chats que YOLO rate. Forme : un jeton `espece:` explicite, jamais une promotion silencieuse. |
| **Ce que le jeton `espece:` AJOUTE** | **MESURÉ** (21/08, `mesure_axe_espece.py`) | Sur la concordance stricte — **3 134** photos — **1 018** ne sont rendues par AUCUN des six mots tapés aujourd'hui ; chat **1 135** à lui seul, soit **47,7 %** de sa concordance (le rappel de SigLIP mesuré le 20/08, vu de l'autre côté). Et sur les **2 186** photos à ESPÈCE SEULE — chiffre retrouvé à l'identique par un second chemin, donc c'en est un — le jeton en atteint **1 499**, dont **409** que rien d'autre ne sort. L'axe rend quelque chose. |
| **Le vrai gain du jeton n'est pas le rappel, c'est la PRÉCISION** | **MESURÉ** (21/08) | Le sémantique remplit TOUJOURS la page : `q=mouton` rend **1 500** photos dont **28** moutons confirmés ; `espece:mouton` en rendrait **32**, tous confirmés par deux regards. Vache : **1 455** des 1 500 sont hors concordance. Le jeton ne gagne pas surtout des photos — il en **retire 1 468**. C'est l'argument qui manquait au 20/08, qui ne comptait que ce qu'on gagne. |
| **Règle ÉLARGIE (poney, brebis, chaton, veau…)** | **REJETÉE, MESURÉE** (21/08) | **+43** photos sur 3 134 (1,4 %) et **+5** sur l'AJOUT : une liste de synonymes à maintenir par espèce, pour un gain qui tient dans le bruit. On câble la STRICTE — le mot et son pluriel, en MOT ENTIER (« château » n'est pas un chat), dans `kw_fr` **ou** `desc`. Les deux sources comptent : le mouton se dit surtout dans `desc` (**38** des 81), le chat presque toujours dans les deux (2 988 sur 3 206). |
| **La concordance du 20/08 n'existait qu'en CHIFFRES** | **CORRIGÉ** (21/08) | Aucun script ne la calculait : ce tableau en portait les résultats, pas la règle. Réécrite en code (`dit_l_espece`, les 82 photos taguées AVEC les faits écartées car elles récitent), elle rend **3 134** contre **3 065** publiés — chat +64, oiseau +44, mouton −29. Même famille, pas le même trait : le trait d'hier ne lisait probablement que `kw_fr`. Un chiffre qu'on ne sait pas reproduire n'est pas une mesure. |
| **Le 5ᵉ axe `espece:` CÂBLÉ (forme A)** | **ADOPTÉ, OBSERVÉ** (21/08) | Un jeton que l'utilisateur ÉCRIT, extrait **avant** les noms — contrairement aux trois autres axes — parce qu'un préfixe n'est ambigu avec rien, alors que le retirer tard le serait. Il filtre sur la CONCORDANCE, calculée à la volée depuis l'ANIMAL_STORE : **4 750** photos portent une détection contre 43 000 entrées d'index, donc **aucun cache à invalider** — une photo taguée il y a dix secondes est filtrable tout de suite. Observé après redémarrage (`code_a_jour` vrai, `verifier_jeton_espece.py`) : `espece:chat` rend **1 500** sur **2 386**, **0 en trop** face à la règle partagée ; `espece:licorne` rend **0** et l'annonce ; `espèce:Chats Luna` rend **198**, les deux axes dits dans la ligne. Le mot NU reste du SENS. Les 82 photos taguées AVEC les faits sont GARDÉES ici alors que le banc les écarte : le banc mesure un accord, et un accord soufflé ne prouve rien ; l'utilisateur, lui, cherche sa photo de chat. |

## Dates de prise de vue

| Idée / piste | Verdict | Raison |
|---|---|---|
| Aplatir les trois dates EXIF en `min()` | **REJETÉ** (13/08) | `ModifyDate` est souvent la date du SCAN : un 1995 numérisé partait en 2005. |
| Corriger les dates d'un nom de SCAN | **REJETÉ** (14/08) | 139 réveillons et 914 à un an sont légitimes ; les 215 à ≥ 4 ans, inséparables. |
| Plancher 1990 des années d'un CHEMIN | **CORRIGÉ → 1900** (14/08) | « 1985 » ne rendait rien ; 716 rendues. |
| `_path_years` lisait le NOM DE FICHIER | **CORRIGÉ** (14/08) | `119-1908_IMG.JPG` sous un dossier 2002 : 38 photos reculées de 94 ans. |
| `DateTimeOriginal` cru sans condition au RENOMMAGE | **CORRIGÉ** (17/08) | `date_de_scan_presumee` : une date postérieure de plus d'un an à TOUTES les années du dossier n'est pas crue (12 → 0). |
| Le garde-fou est SYMÉTRIQUE | **REJETÉ** (17/08) | Une date ANTÉRIEURE au dossier est l'EXIF qui corrige un import : **1 347** contre 72. |
| Corriger `taken` en BASE | **REJETÉ** (19/08) | 72 corrections face à **1 347** antérieures légitimes — `taken` est une LECTURE de l'EXIF : la correction est une VUE. |
| Le garde-fou de la date de SCAN passe à la LECTURE | **ADOPTÉ, OBSERVÉ** (19/08) | `faits_vue.date_credible` INJECTÉ dans `meme_jour.epoch_precis` : une implémentation pour le tri, le filtre, « même jour » et le fait. **70** photos perdent une date précise fausse. Observé : `Photos Papa\1983\20150810_…` a quitté le « 10 août ». |
| `_best_time` était une COPIE de `epoch_precis` | **CORRIGÉ** (19/08) | Sa branche 1 n'avait pas suivi le garde-fou : la galerie datait de 2006 ce que la recherche datait de 1985. |
| Écrire `None` pour tout un lot ExifTool | **REJETÉ** (13/08) | Un lot raté (NAS muet) est indiscernable d'un vide : on n'écrit que si ExifTool a PARLÉ. |
| Planchers 1990 restants ; plafond 2100 | **PARKÉ chiffré** (15 et 19/08) | 0 et 7 photos, couplés ; `22082010141.jpg` se lit « 2082 » — 72 en base, coût 0. |

## Interface

> Sorti le 07/09 dans **`eval/DECISIONS_UI.md`** (découpage par
> DOMAINE, choix de Mike — même geste que l'outillage le 20/08).
> Composants, cibles, contraste, chips, ce qu'une grille compte.
> La règle de VISIBILITÉ, elle, reste ici : elle décide qui voit
> quoi, pas de quoi l'écran a l'air.

## Gouvernance de ce fichier

| Idée / piste | Verdict | Raison |
|---|---|---|
| Sortir les verdicts clos dans une archive PAR STATUT | **REJETÉ** (19/08, choix de Mike) | Précédent de `METHODE.md` (16/08), mais elle coupe un même domaine en deux selon l'ÂGE : il faudrait relire les deux fichiers, et le réflexe « ne rien re-proposer » suppose de tout voir d'un coup d'œil. |
| **Découper par DOMAINE** | **ADOPTÉ** (20/08, choix de Mike) | Ce que l'archive par statut n'avait pas : le réflexe est lui-même par domaine. L'OUTILLAGE part dans `docs/DECISIONS_OUTILLAGE.md` — qui travaille la recherche n'a jamais besoin de savoir pourquoi `taskkill` a échoué, et réciproquement. Chaque fichier reste COMPLET pour son propre réflexe. |
| **Budget 9 000 → 100 000 octets** | **ADOPTÉ** (19 → 23/08, choix de Mike, quatre relèvements — détail dans `git log`) | Chaque seuil a été consommé avant d'avoir servi, et la seule marge restante était la PRÉCISION des raisons — or c'est elle que le seuil protégeait. **Ce que le seuil ne fait plus** : il ne protège plus contre le récit. Ce qui en protège, c'est la FORME — un tableau, une ligne par verdict, un chiffre ou une réfutation dans chaque raison — et le RÔLE de chaque fichier, écrit en tête de `TRACKING_MD`. Ce que le seuil garde : rattraper un emballement franc, un fichier qui double, une doc qui devient un journal. |
| **Budget 100 000 → 125 000 octets** ET **second découpage par domaine** | **ADOPTÉS ENSEMBLE** (08/09, choix de Mike) | Le carnet était à **96 %** (96 019) et le découpage de l'ÉCRAN, fait la veille, avait libéré 8,5 Ko **consommés en vingt-quatre heures**. C'est le CINQUIÈME relèvement de seuil, et les quatre précédents ont tous été consommés avant d'avoir servi : il ne pouvait donc pas partir seul. Le TAGGING sort dans `eval/DECISIONS_TAGGING.md` — 31,6 Ko, la plus grosse section homogène, déjà écrite comme une section, et le réflexe est net (qui travaille la visibilité n'a pas besoin du prompt de tagging). **Résultat mesuré : 96 019 → 64 729 octets, soit 52 % du nouveau budget** au lieu de 96 % de l'ancien. La troisième voie, condenser les verdicts anciens, reste disponible et n'a PAS été utilisée : elle demande de relire pour décider quoi perdre, et un carnet qu'on n'a plus besoin de raccourcir ne se raccourcit pas au chausse-pied. |
| **Un verdict rendu ne s'attend plus — l'onglet Sensibles ressuscitait 67 dossiers clos** | **MESURÉ ET CORRIGÉ, OBSERVÉ EN RÉEL** (09/09) | Mike a trié les 213 candidates dans l'après-midi et m'a dit « sauf 1 ». J'ai vérifié le compte plutôt que de le croire sur parole : **l'API en annonçait 68**. Le détail dit tout — **60 dans `.corbeille-effacements`, 7 dans un `PRIVE`, 1 vraiment en attente**. Sa lecture était juste ; c'est l'application qui mentait. **Cause** : le drapeau `sensible` reste posé quand la photo DÉMÉNAGE. Mettre à la corbeille et ranger en privé re-clent l'entrée vers son nouveau chemin sans effacer la marque, donc la photo continue d'« attendre un verdict » qu'elle a déjà reçu. Rien ne se voyait, parce que la page retire les fiches à l'écran au fur et à mesure — **au rechargement suivant, 67 documents déjà jugés seraient revenus dans l'onglet, vignettes et chemins complets compris**, l'exact contraire de ce que l'onglet promet, et sur des relevés bancaires. **Le correctif est une règle de CHEMIN, `_verdict_deja_rendu`, appliquée par la liste ET par le compteur de candidates** : une photo sortie du fonds (`_is_hidden_path` : un composant commençant par `.`, `@` ou `#`) ou rangée dans un `PRIVE` (`visibilite.est_prive`) a reçu son verdict par construction. **Et surtout, on n'efface PAS le drapeau au moment du geste** — c'était la solution évidente et elle est moins bonne : une photo restaurée de la corbeille revient à son ancien chemin et **redevient en attente toute seule**, ce qu'un drapeau effacé ne saurait plus dire. La règle de chemin garde la réversibilité que le projet exige partout ailleurs. **Vérifié après redémarrage, sur le serveur vivant : 68 → 1**, zéro corbeille, zéro privé, et le compteur de candidates dit 1 lui aussi — les deux routes s'accordent enfin. **La leçon** : un compteur qui ne bouge pas et un utilisateur qui dit « c'est fini » peuvent être d'accord *à l'écran* et faux *dans les données* ; le seul moyen de le savoir est de demander à l'API, pas à la page. |
| **`animal:luna` en minuscule : SANS OBJET, et le système le disait déjà** | **MESURÉ, POINT CLOS** (09/09) | La liste d'audit portait « `animal:luna` en minuscule sur 3 photos à côté de `animal:Luna` sur 355 ». Mesuré aujourd'hui : **2 photos**, pas 3. Et surtout — **rien ne lit la casse de ce tag.** `_kw_has` compare en minuscules, et sa docstring dit pourquoi, écrite bien avant cette note : *« Indispensable car les tags importés d'un fichier et ceux écrits par l'app peuvent différer de casse. »* `SubjectStore.confirm` clé la fiche sur `name.lower()` — **l'identité d'un sujet EST son nom en minuscules**, le nom d'affichage vit à part dans la fiche ; `pets_list` replie aussi ; et la recherche rend **364 résultats pour `animal:luna` comme pour `animal:Luna`**, mesuré. Le tag n'est donc autoritatif nulle part : la fiche l'est. **Ce n'était pas une incohérence à réparer, c'était exactement le cas que le modèle a été conçu pour absorber** — deux sources de tags (les fichiers, l'application) qu'on ne peut pas forcer à s'accorder. La « corriger » aurait écrit du XMP dans deux photos de Mike — donc touché un nom humain, l'invariant le mieux protégé du projet — pour une uniformité qu'aucun consommateur ne lit. **La leçon, et elle vaut au-delà de ce point** : avant de réparer une incohérence de données, chercher qui la LIT. Si personne ne la lit, ce n'est pas une incohérence, c'est une redondance que le modèle tolère par construction — et l'écriture qu'on s'apprêtait à faire coûte plus que ce qu'elle rapporte. |
| **La compression HTTP (O11) : deux tiers des octets, pour vingt lignes** | **FAIT, MESURÉ SUR LE FIL** (09/09) | L'audit interne la donnait « effort S, ×5–10 sur les JSON/HTML de plusieurs Mo ». **Mesuré après coup dans le navigateur, en lisant `encodedBodySize` contre `decodedBodySize` — donc ce qui a VRAIMENT traversé le réseau, pas une estimation** : `/files` passe de **157 à 48 ko (−69 %)**, `/pets` de **88 à 29 ko (−67 %)**. Sur un NAS domestique et un téléphone en wifi, c'est la différence qui se sent. **Un seul endroit compresse**, `_repondre`, par lequel passent `_send` (les 151 réponses JSON) et `_send_html` (les pages) : la compression touche le `Content-Length`, et deux endroits qui l'écrivent finiraient par ne plus dire la même chose — un `Content-Length` faux coupe la réponse au milieu **sans erreur visible**. Les médias et les vignettes gardent leur chemin propre : un JPEG est déjà compressé, le gzipper brûlerait du CPU pour grossir la réponse. **Trois gardes, chacun pour une panne connue** : un seuil de 4 Ko (sous lequel l'en-tête coûte plus que le gain) ; la version compressée n'est gardée **que si elle est plus petite** ; et `Vary: Accept-Encoding` est envoyé **même quand on n'a pas compressé** — son oubli est la panne classique de la compression conditionnelle, celle qui fait servir par un cache la mauvaise version au mauvais client, et qui ne se voit jamais en développement. 7 tests (`test_compression.py`). **Un défaut du banc lui-même, corrigé dans la foulée et qui vaut d'être noté** : la première version cherchait « Content-Length » dans le corps de la fonction et le trouvait **dans ma propre docstring**, rouge sur un code juste. C'est la troisième fois cette semaine qu'une assertion lit un commentaire au lieu du code — l'assertion porte désormais sur `send_header('Content-Length'`. |
| **L'admin DÉPOSE dans le privé de chacun, il n'y FOUILLE pas** | **TRANCHÉ PAR MIKE le 10/09/2026** — « j'ai le droit de déposer dans le privé de tout le monde, mais uniquement parce que je suis administrateur » | La dernière photo de l'onglet /sensibles refusait « Rendre privée » à chaque clic : *« Impossible : Fichier introuvable. »* — sur une photo dont la **vignette était à l'écran**. Deux défauts empilés. **(1) Le geste était réellement interdit** : la photo est dans `Photos Flo`, « Rendre privée » la déplace vers le PRIVE de son PROPRIÉTAIRE (`cible_prive`), donc `Photos Flo/PRIVE`, et le chantier 17 ferme ce dossier même à l'admin. D'où une **dissymétrie que personne n'avait voulue : l'admin pouvait EFFACER la photo de Flo, mais pas la PROTÉGER.** Une règle de confidentialité qui laisse détruire et interdit d'abriter protège le mauvais geste. Mike tranche : le dépôt est ouvert à l'admin, la lecture reste fermée — `peut_ecrire(..., depot=True)`, exception **nommée**, passée en ARGUMENT au garde de `mkdir` et `move` (jamais un drapeau global : le serveur est threadé, et un « dépôt en cours » partagé ouvrirait le PRIVE d'autrui à la requête d'à côté). Contrôle négatif mesuré sur la vraie clé : sans `depot`, le même chemin reste refusé 404. **(2) Le message mentait.** « Fichier introuvable » existe pour ne pas révéler l'existence d'une photo à qui ne peut pas la voir — il est faux dès que l'onglet l'a mise sous les yeux de celui qui clique. Le refus se nomme maintenant AVANT de toucher au disque (`refus_rendre_privee`), et `/api/sensibles` le rend au client (`prive_refus`) pour que la page **éteigne le bouton et dise pourquoi** — même défaut, même correction que l'étape 3 du bat 50 la veille : *une option qui ne peut jamais aboutir est une promesse que l'outil ne tiendra pas*. **Un ordre a compté** : le banc a attrapé du premier coup que demander `cible_prive` avant la visibilité répondait « déjà dans un dossier privé » à quelqu'un qui n'avait pas le droit de savoir que la photo existe — un refus exact qui **confirme une existence**. La visibilité passe en premier. Mesuré sur la machine (`verifier_depot_prive.py`) : la photo bloquée **ABOUTIT**, les 7 autres sont déjà privées. |
| **O14 — `save()` re-empreintait 44 121 entrées pour ne rien trouver, 627 ms sous le verrou, une fois PAR DOSSIER** | **MESURÉ le 10/09** (`mesure_reconciliation.py`, sur une copie de la base) | `SqliteStore.save()` appelle `_reconcilier()`, qui relit l'index ENTIER — `dict(e)`, `json.dumps`, `blake2b` par entrée — pour trouver les mutations **profondes**, que `TrackedEntry` ne signale pas (`e['faits']['lieu'] = …` ne passe pas par `__setitem__` de premier niveau). Le chiffre : **627,2 ms à vide, 0 écriture, 14,2 µs par entrée** ; le flush signalé fait le même travail utile en **0,1 ms** — **6 547 ×** moins cher. Et le point d'appel qui fait mal n'était pas le tagging (il passe par `set()`, donc par le chemin rapide) : c'est **`_sync_dir`, qui termine CHAQUE DOSSIER par un `STORE.save()`** — le chemin même qui avait gelé l'interface le 06/09. **La garantie n'a pas bougé** : `save()` reste, et `flush()` est un nom PUBLIC distinct, dont la limite est écrite noir sur blanc et tenue par un banc (`test_flush_o14.py` : `flush()` persiste une mutation de premier niveau, **ne voit pas** une mutation profonde, `save()` la voit toujours). Trois points d'appel convertis, **chacun avec sa preuve écrite à côté** : `_sync_dir` (tout passe par `set(..., save=False)`), `_cat_auto_pass` (`_index_add_person` finit par `e['kw_fr'] = kw`), `reembed_one_batch` (`e['reemb'] = 1`). Le repli JSON `TagStore` porte le **même nom** — sinon supprimer `photos.db`, censé être un retour arrière trivial, ferait tomber le scan. *Dans le doute, `save()` : se tromper d'un côté perd une écriture, de l'autre ne coûte que du temps.* |
| **O15 — 722 Mo de vignettes en cache, dont 541 Mo de morts (75 %)** | **MESURÉ le 10/09** (`mesure_caches_vignettes.py`) | Trois caches locaux, et le code disait lui-même depuis des semaines : *« les anciennes vignettes orphelines restent sur disque (purge maintenance : à traiter avec O15) »*. Le nom de fichier est une empreinte de ce qui DÉFINIT la vignette — `md5(clé\|taille\|mtime)` pour une photo, `md5(clé\|index\|bbox)` pour un visage — **donc tout ce qui change le mtime ou la bbox l'orpheline**, et écrire les tags XMP change le mtime. Le compte : `photo_thumbs` 7 737 fichiers / 450,7 Mo dont **56 % morts** ; `face_thumbs` 34 771 / 224,1 Mo dont **83 %** (le ré-embedding déplace les bboxes) ; `animal_thumbs` 4 819 / 47,2 Mo dont **74 %**. **Ce que « réversible » veut dire ici, et pourquoi ce n'est PAS une corbeille** : une vignette n'est pas une donnée, c'est un calcul mis de côté, qui se refait à la première demande en lisant l'original. Déplacer 541 Mo vers une corbeille occuperait la place deux fois sans rien rendre de plus — *la réversibilité, ici, c'est la régénération*, et un journal dit ce qui est parti. **Les deux garde-fous visent MON code, pas les fichiers de Mike** : (1) si moins de 5 % des fichiers d'un dossier correspondent à un nom vivant, le dossier est **refusé en bloc** — ce n'est pas un cache périmé, c'est ma formule qui a cessé de parler la même langue que `server.py`, et tout effacer serait le pire geste possible au moment où l'on comprend le moins ; (2) rien de moins de 7 jours n'est touché, parce qu'une formule fausse se trompe **d'abord** sur les vignettes qu'on vient de créer, celles qui servent maintenant. 7 bancs, dont un par verrou. Un défaut du banc de mesure attrapé au premier lancement, et qui vaut d'être noté : `sum(t[0] for t in morts)` reprenait le `t` d'une compréhension voisine — `t[0]` était le NOM, et la somme d'entiers additionnait des chaînes. |
| **Le « N » de Mike a sauvé 282 Mo — et mon garde-fou n'y était pour rien** | **MESURÉ le 10/09** (`verifier_formule_vignettes.py`) | À l'aperçu du bat 51, les taux de reconnaissance sont bas : `photo_thumbs` 33 %, `face_thumbs` 17 %, `animal_thumbs` 26 %. Le plancher de l'outil est à 5 % : **il n'a rien refusé**, et il proposait d'effacer 510 Mo. Mike a répondu **N**. Le banc écrit ensuite sépare les deux histoires par un fait que personne ne maquille — **l'âge** : si la formule suit le serveur, les vignettes les plus JEUNES sont reconnues. Verdict : `face_thumbs` et `animal_thumbs` **100 % des 50 plus jeunes reconnues** (vétusté réelle), `photo_thumbs` **16 %** — aussi bas que ses plus vieilles. **La cause n'est PAS une formule fausse** : 2 561 noms y sont reconnus, et on ne tombe pas par accident sur des milliers de md5 justes. C'est que `photo_thumbs` est nommé sur le **MTIME**, que la campagne de retag réécrit en continu : ce cache **se périme plus vite qu'il ne se remplit**, et son âge ne dit plus rien. Les deux autres sont nommés sur la BBOX, que rien ne réécrit en ce moment. **Trois leçons.** (1) *Un plancher calibré sur la panne totale ne voit pas la panne partielle* — 5 % n'attrape qu'une dérive complète. (2) *Un critère de sûreté n'est valable que là où son hypothèse tient* : l'âge suppose qu'une vignette vieillit sans être invalidée, ce qui est faux pendant une campagne. L'instrument le DIT maintenant, au lieu de crier à la dérive. (3) **Le premier verdict de mon propre banc était faux** (« DÉRIVE ») et je l'ai corrigé plutôt que de le croire : *un instrument qui juge doit pouvoir être jugé*. Le bat 51 traite désormais les deux caches qu'on sait lire — 32 458 fichiers, 227 Mo — et `photo_thumbs` revient après la campagne (`--dossiers photo_thumbs`), quand son âge redeviendra lisible. |
| **Le cache de vignettes est ÉTEINT pendant la campagne — 4,5 % de taux de service — et c'est nous qui l'éteignons** | **MESURÉ le 10/09** (`mesure_service_vignettes.py`) | Le compte de O15 avait laissé une question plus intéressante que ses 722 Mo : `photo_thumbs` ne portait que **2 561 noms vivants**. Mesuré à l'endroit : **1 995 photos sur 44 604 (4,5 %) ont une vignette 512 utilisable**, et **80 (0,2 %)** une 1600. Autrement dit **95 % des cases de galerie relisent l'original sur le NAS — 2 à 6 Mo au lieu de ~50 Ko**, ce que l'audit O1 avait justement voulu supprimer. La cause : une vignette s'appelle `md5(clé\|taille\|MTIME)`, et **écrire un tag XMP change le mtime sans changer un seul pixel**. La campagne en périme **6 524 par jour** (14,6 % de l'index en 24 h, 65,5 % en 7 jours) ; la galerie n'en refait que ce qu'on regarde. Le cache perd la course. Et aucun champ de l'index ne décrit les PIXELS : **le cache est indexé sur une identité de FICHIER pour retrouver une image qui, elle, n'a pas bougé** — c'est la phrase qui contient tout le défaut. Proposition portée à `QUESTIONS_MIKE.md`, en deux gestes solidaires : (1) séparer le NOM (`md5(clé\|taille)`) de la VALIDITÉ (le mtime de la source, tamponné dans le mtime du fichier de cache) — un seul fichier par (photo, taille), donc **plus aucun orphelin par construction, et O15 disparaît à sa racine** ; (2) **ne plus jeter ce qu'on sait intact** — c'est le serveur qui écrit les XMP, il sait à cet instant que seules les métadonnées ont bougé, et un `os.utime` sur la vignette remplace une relecture de 2 à 6 Mo. **Leçon de méthode** : le compte de O15 mesurait le POIDS d'un cache ; la vraie question était son TAUX DE SERVICE, et elle ne se voyait pas dans les octets. *Un cache se juge à ce qu'il évite, pas à ce qu'il occupe.* |
| **Le nom dit QUELLE vignette, le tampon dit si elle est À JOUR** | **TRANCHÉ PAR MIKE le 10/09** (« ok, je te suis ! ») **et livré le même soir** | Réponse au cache éteint à 4,5 %. **Geste 1 — séparer le nom de la validité** : une vignette s'appelle `md5(clé\|taille)`, sans mtime, et porte dans son propre mtime celui de la photo dont elle vient ; valide si les deux concordent, **à la seconde entière** (source sur SMB, cache en local : deux systèmes de fichiers, une égalité de flottants y serait un piège). Conséquence : **un seul fichier par (photo, taille)** — une vignette périmée est ÉCRASÉE, pas dupliquée, donc **plus d'orphelin par construction et O15 disparaît à sa racine**. Il en reste un cas, dit honnêtement : renommer une photo change sa clé donc son nom de vignette, et l'ancienne meurt ; c'est rare devant les écritures de tag, et le bat 51 sait encore la ramasser. **Geste 2 — ne plus jeter ce qu'on sait intact** : c'est le serveur qui écrit les XMP, il sait à cet instant que seules les métadonnées ont bougé ; un `os.utime` remplace une relecture de 2 à 6 Mo sur le NAS plus un réencodage. **Le banc qui compte le plus n'est aucun des deux** : il VERROUILLE la liste des appelants de `_retamponner_vignettes` en exigeant que chacun écrive `write_metadata` ou `write_person_tags` — appelé après une vraie modification d'image, ce geste servirait une vignette périmée, et un copier-coller suffirait à l'y mettre. 12 bancs, verts sur la machine ; serveur redémarré 21:47:20, zéro traceback. **Migration** : tous les fichiers de l'ancien format deviennent orphelins d'un coup — le bat 51 le traite avec `--formule-changee RAISON`, qui met le plancher à zéro **mais exige une raison écrite et la journalise**. Même règle que `force=raison` côté git : *un contrôle qu'on lève sans dire pourquoi n'est plus un contrôle.* |
| **Le correctif du cache : posé sur DEUX chemins, il en fallait TROIS — et c'est l'observation en réel qui l'a dit** | **MESURÉ le 10/09 au soir, avant/après sur la machine** | J'avais patché `retro_write_metadata` et le worker de tags nommés. **Le chemin principal manquait** : `tagger_worker` écrit les XMP en ligne puis relève `_stat_of(path)` — c'est lui qui change le mtime de ~6 500 photos par jour pendant la campagne, quand `retro_write_metadata` ne tourne qu'avec la maintenance. **Le banc existant ne pouvait pas le voir** : il verrouillait la liste des appelants en vérifiant que chacun écrit bien des métadonnées — *une liste blanche ne compte pas les absents*. Trouvé en ouvrant une galerie et en lisant le journal, pas en relisant le code. L'invariant est réécrit dans sa bonne formulation, et il compte les absents : **toute fonction qui écrit des métadonnées ET enregistre le nouveau mtime doit redater les vignettes** — c'est l'enregistrement du mtime qui périme la vignette, donc c'est lui qui porte l'obligation, et une enveloppe qui ne fait que relayer l'appel (`write_person_tag` au singulier) s'exempte toute seule, sans liste d'exception. **LA PREUVE EN RÉEL, en trois temps** : (1) galerie de `Photos Mike\2023` ouverte, `/api/thumb` répond 200 en série, 581 vignettes au nouveau format créées ; (2) après le correctif du tagueur et un redémarrage, **746 vignettes 512 utilisables** ; (3) cinq minutes plus tard, pendant lesquelles **29 photos de ce dossier ont été retaguées** : **toujours 746, et 8 483 fichiers dans `photo_thumbs` — inchangé**. Sous l'ancien code, ces 29 retags auraient jeté jusqu'à 29 vignettes et laissé 29 orphelines derrière. |
| **Celui qui remplace nettoie — O15 fermé par construction sur les découpes aussi** | **MESURÉ puis livré le 10/09** | Les découpes de visages et d'animaux sont nommées `md5(clé\|index\|bbox)` : ici le nom EST dérivé du contenu, et c'est juste — la découpe dépend du cadre. Mais un cadre qui change abandonne son fichier : **83 % de `face_thumbs`** (192,9 Mo) et **74 % de `animal_thumbs`** étaient morts. La réponse n'est donc pas de changer le nom, comme pour les vignettes de photo, mais de faire nettoyer **celui qui remplace**. C'est le pendant exact du correctif des vignettes, dans l'autre sens : là nous savions que l'image n'avait pas changé et nous gardions ; ici nous savons que le cadre a changé et nous jetons. *Dans les deux cas c'est l'ÉCRIVAIN qui sait, donc c'est lui qui agit.* **Et l'analyse a réduit le chantier avant de l'ouvrir** : sur les cinq endroits qui écrivent une liste de détections, **trois n'écrivent que s'il n'y avait rien** (`FACE_STORE.has(name)`, `fe is None`) — ils n'ont rien à nettoyer, et leur ajouter du nettoyage aurait été du bruit qu'on ne saurait plus justifier. Restent DEUX sites : `reembed_one_batch`, qui déplace les cadres photo par photo (les cadres inchangés gardent leur découpe — on ne refait pas pour rien), et `migrate_animal_pipeline`, qui remet le magasin à zéro et vide donc le cache en entier. 12 bancs, dont un qui tient **la raison des trois exemptés** : sans lui, un lecteur pressé ajouterait du nettoyage partout. Serveur redémarré 22:42:40, zéro traceback. |

> **Méthode : `eval/METHODE.md`** — ici *ce qui a été tranché*, là *comment*.
